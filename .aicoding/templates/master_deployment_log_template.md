# [项目名称] 部署记录（主文档模板）

> 本文档用于"追加式"记录每次部署；详细步骤请参考每次变更目录内的 `deployment.md`。

| 日期 | 环境 | 版本(tag/commit) | 执行人 | 变更概要 | 包含CR | 验证方式 | 回滚情况 | 备注 |
|---|---|---|---|---|---|---|---|---|
| YYYY-MM-DD | STAGING |  |  |  |  |  |  |  |

> **包含CR字段说明（P2增强）**：
> - **包含CR**：记录本次部署关联的 CR-ID，多个 CR 用逗号分隔
> - 示例：CR-20260208-001, CR-20260208-002
> - 用于追溯：部署时能快速定位是哪个 CR 导致的变更
> - Deployment 门禁时，验证"包含CR"是否与 deployment.md 的"本次上线CR列表"一致

## 详细记录（可选）

### YYYY-MM-DD / PROD / v2.1

> **部署记录块格式规范（🔴 MUST，硬规则）**：
> - 每次部署追加一个记录块，格式为二级标题：`## <版本号> / <环境>` 或 `### YYYY-MM-DD / <环境> / <版本号>`
> - Deployment 门禁时，根据 deployment.md 的"部署版本"或"部署日期"定位对应记录块
> - **匹配失败规则（🔴 MUST，零容忍）**：如找不到匹配块，**必须失败**并提示按规范追加记录块

#### 基本信息
- **部署版本**：v2.1
- **部署日期**：YYYY-MM-DD
- **部署环境**：PROD / STAGING / DEV
- **执行人**：

#### 包含CR（P2增强）
> **填写规则**：列出本次部署包含的所有 CR-ID

| CR-ID | 标题 | 状态 |
|-------|------|------|
| CR-YYYYMMDD-001 | 用户登录功能 | Implemented |
| CR-YYYYMMDD-002 | 接口优化 | Implemented |

#### 关联变更文档
- `docs/<版本号>/deployment.md`
- `docs/<版本号>/cr/CR-YYYYMMDD-001.md`
- `docs/<版本号>/cr/CR-YYYYMMDD-002.md`

#### 关键步骤/特殊操作

1. **前置检查**
   - [ ] 基线版本验证
   - [ ] 备份完成
   - [ ] 回滚方案准备

2. **部署步骤**
   - 步骤1：...
   - 步骤2：...

3. **验证结果**
   - [ ] 功能验证
   - [ ] 回归测试
   - [ ] 性能验证（如适用）

#### 风险与处理
- **风险点1**：
  - 处理方式：
- **回滚条件**：
  - 触发条件：
  - 回滚步骤：

---
