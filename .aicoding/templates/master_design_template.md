# [系统/项目名称] 技术方案设计（主文档模板）

> 说明：本主文档建议“截面式”维护**当前系统**的架构全景与关键设计决策，并在末尾按版本“追加式”维护变更记录。  
> 版本/迭代的详细技术方案请写在 `docs/<版本号>/design.md`（模板：`.aicoding/templates/design_template.md`）。

## 文档元信息
| 项 | 值 |
|---|---|
| 状态 | Draft / Review / Approved |
| Owner |  |
| 评审 |  |
| 最后更新 | YYYY-MM-DD |
| 架构基线 | 主分支（`main`/`master`）/ tag（如 `v1.0`） |
| 适用范围 | 生产/预发/测试（按实际补齐） |
| 关联主文档 | `docs/系统功能说明书.md`、`docs/接口文档.md`、`docs/用户手册.md`、`docs/部署记录.md` |

## 变更总览
| 版本号 | 日期 | 概要 | 关联变更文档 |
|---|---|---|---|
| `<版本号>` | YYYY-MM-DD | 初始化/重大架构调整 | `docs/<版本号>/design.md` |

---

## 0. 摘要（Executive Summary）
用 10~20 行说明：
- 系统解决什么问题、服务哪些用户/角色
- 端到端主链路（输入→处理→输出）
- 最重要的 3~5 个质量目标（安全/可用性/可追溯/成本等）
- 当前架构形态与关键约束（单机/分布式、存储选型、网络边界等）

## 1. 背景、目标、非目标与约束

### 1.1 背景与问题
- 现状与痛点：
- 历史包袱/遗留约束（如：存量数据、协议兼容、合规限制）：

### 1.2 目标（Goals）
- G1 ...
- G2 ...

### 1.3 非目标（Non-Goals）
- N1 ...
- N2 ...

### 1.4 关键约束（Constraints）
- C1 法规/合规（如：等保、隐私、数据出境、审计）：
- C2 运行环境/部署形态（如：内网、K8s、单机、边缘）：
- C3 资源与成本（如：预算、许可证、GPU不可用）：
- C4 交付节奏（如：必须分期、可灰度）：

## 2. 术语与口径（Glossary）
对关键术语、缩写、角色、指标口径做统一定义（避免各版本漂移）。

**ID 前缀规则**：详见 `.aicoding/STRUCTURE.md` 统一定义。

## 3. 系统上下文与边界（Context）

### 3.1 系统边界
- In Scope：
- Out of Scope：
- 责任边界（谁负责什么，发生问题找谁）：

### 3.2 外部依赖清单（含 SLA/失败模式）
| 依赖方/系统 | 用途 | 协议 | SLA/SLO | 失败模式 | 降级/兜底 | Owner |
|---|---|---|---|---|---|---|
|  |  | HTTP/gRPC/MQ/DB |  | 超时/限流/返回异常 | 重试/缓存/停止服务/人工兜底 |  |

### 3.3 信任边界与数据分类
- 信任边界（内网/公网、VPC、零信任等）：
- 数据分类分级（公开/内部/敏感/受限），以及存储/传输要求：
- 关键审计对象（谁在什么时间对什么做了什么）：

## 4. 架构总览（多视图 / C4 建议）

### 4.1 Context View（系统上下文图）
（可用 Mermaid/图片；尽量包含外部系统、用户、边界。）

### 4.2 Container View（容器/进程视图）
- 前端/后端/异步作业/网关/存储/搜索/缓存等的职责与边界
- 网络拓扑与端口暴露策略（哪些对外、哪些仅内网）

### 4.3 Component View（组件/模块视图）
- 关键模块拆分与依赖方向
- 模块间接口（同步/异步）与契约治理（版本/兼容/废弃策略）

### 4.4 Deployment View（部署视图）
- 环境划分（dev/staging/prod）
- 部署单元（容器/VM/Serverless）
- 资源与配额（CPU/内存/磁盘/网络）
- 高可用与容灾（多 AZ/多活/备份恢复）

## 5. 关键设计决策（ADR 摘要）
> 建议把“做/不做/为什么”的结论沉淀为 ADR；本节仅做汇总索引。

| ADR-ID | 决策主题 | 决策结论 | 状态 | 日期 | 影响范围 | 备注/链接 |
|---|---|---|---|---|---|---|
| ADR-001 |  |  | Proposed/Accepted/Superseded | YYYY-MM-DD |  |  |

## 6. 核心数据与一致性（Data & Consistency）

### 6.1 领域模型与系统主数据（System of Record）
- 核心实体与关系（可贴 ER 图/领域模型图）：
- 哪些数据由本系统负责“最终一致/强一致”的真相源：
- ID 生成策略（全局唯一、可排序、可追踪）：

### 6.2 数据一致性与并发策略
- 一致性策略：强一致/最终一致/读写分离
- 幂等：幂等键、去重窗口、重放处理
- 并发控制：乐观锁/悲观锁/版本号

### 6.3 数据生命周期
- 保留与归档策略（Retention/TTL）：
- 备份与恢复（RPO/RTO）：
- 数据清理与合规删除（如适用）：

## 7. 集成与接口策略（API/Event）

### 7.1 API 风格与通用约定
- 协议（REST/gRPC/GraphQL）：
- 认证鉴权（JWT/OAuth2/mTLS）：
- 错误码与返回格式统一：
- 分页/排序/过滤约定：
- 超时/重试与幂等策略：

### 7.2 事件与异步（如适用）
- Topic/Queue 清单：
- 投递语义（at-least-once/exactly-once）：
- 顺序性与幂等：
- DLQ 与补偿：

## 8. 安全、隐私与合规（Security/Privacy/Compliance）

### 8.1 认证与授权
- 角色模型与权限资源粒度：
- 管理面与数据面隔离（如适用）：

### 8.2 数据安全
- 传输加密（TLS/mTLS）：
- 存储加密（KMS/磁盘/字段级）：
- 脱敏与最小化：
- 密钥与 Secret 管理（禁止写入仓库）：

### 8.3 威胁建模与缓解（简表）
| 威胁/攻击面 | 风险 | 缓解措施 | 验证方式 |
|---|---|---|---|
|  |  |  | 测试/审计/演练 |

### 8.4 合规要求（如适用）
- 等保/ISO/审计留痕：
- 隐私（PII/敏感数据）处理策略：
- 数据出境与第三方共享：

## 9. 可靠性、可观测性与运维（Reliability/Observability/Ops）

### 9.1 可靠性策略
- 超时/重试/限流/熔断/降级：
- 关键链路的兜底与人工回路：
- 容灾与演练（含频率与验证证据）：

### 9.2 可观测性
- 日志：结构化字段、Trace/Request ID、审计日志
- 指标：核心 SLIs（延迟/错误率/吞吐/饱和度），SLO（如适用）
- 链路追踪：采样策略、跨服务传播
- 告警：阈值、去噪、升级策略（值班/通知）

### 9.3 运维与可支持性（Supportability）
- Runbook（常见故障处理步骤）
- 管理工具/后台（如适用）
- 变更管理：灰度、开关、回滚、公告

## 10. 性能与容量（Performance/Capacity）
- 性能目标与口径（P95/P99、窗口、样本量）：
- 容量规划（QPS/并发/数据量增长、峰值因子）：
- 压测策略与基线：
- 缓存/索引/分片/队列等扩展策略：

## 11. 发布、兼容与演进（Release/Evolution）
- API 版本策略与废弃策略：
- 数据迁移与回滚策略：
- 向后兼容要求（接口/数据/配置）：
- Feature Flag 规则与默认状态：
- Roadmap（按季度/版本）：

## 12. 成本与资源（Cost）
- 主要成本项：计算/存储/带宽/许可证/第三方 API
- 成本驱动因素与优化空间：
- 成本告警与预算控制（如适用）：

## 13. 风险与开放问题

### 13.1 风险清单
| 风险 | 影响 | 概率 | 应对措施 | Owner |
|---|---|---|---|---|
|  |  | 高/中/低 |  |  |

### 13.2 开放问题（持续更新）
- [ ] ...

---

## 变更记录（追加）

> **规则2验证依赖（🔴 MUST，CR场景）**：
> - 每次更新主文档时，必须在下表中追加一条记录
> - 表头必须包含：日期、版本、CR-ID、变更说明、变更人
> - CR-ID 列必须填写完整的 CR-YYYYMMDD-NNN 格式
> - 如无关联 CR（如初始版本），CR-ID 填写 "-"
> - Deployment 门禁验证时，检查 CR-ID 是否存在于变更记录表中

| 日期 | 版本 | CR-ID | 变更说明 | 变更人 |
|------|------|-------|---------|--------|
| YYYY-MM-DD | v1.0 | - | 初始版本 | [作者] |
| YYYY-MM-DD | vX.Y | CR-YYYYMMDD-NNN | [变更说明] | [变更人] |
