# [项目名称] 接口文档（主文档模板）

## 文档元信息
| 项 | 值 |
|---|---|
| 状态 | Draft / Review / Approved |
| Owner |  |
| 最后更新 | YYYY-MM-DD |

## 1. 概述
- Base URL：
- 版本策略：
- 认证与授权：
- 幂等策略：
- 限流/超时/重试：

## 2. 通用约定
### 2.1 请求头/追踪
- `X-Request-Id`：
- `Trace-Id`：

### 2.2 错误码与返回格式
（建议与 `docs/<版本号>/requirements.md` 的“6.2 错误码与提示语”保持一致）

## 3. 接口列表（截面式）
按模块列出当前支持的 API（可链接到 OpenAPI/Swagger）：
- 模块A
  - API-001：`METHOD /path` — 摘要
  - API-002：`METHOD /path` — 摘要

## 4. 废弃接口
| API-ID | 路径 | 废弃日期 | 废弃原因 | 替代方案 | 计划移除日期 |
|---|---|---|---|---|---|
|  |  | YYYY-MM-DD |  |  |  |

## 5. 变更记录

> **规则2验证依赖（🔴 MUST，CR场景）**：
> - 每次更新主文档时，必须在下表中追加一条记录
> - 表头必须包含：日期、版本、CR-ID、变更说明、变更人
> - CR-ID 列必须填写完整的 CR-YYYYMMDD-NNN 格式
> - 如无关联 CR（如初始版本），CR-ID 填写 "-"
> - **接口文档特殊说明（🔴 永久允许兜底）**：除变更记录表外，也允许在对应 API 条目备注中包含本 CR-ID（如"变更于：CR-20260208-001"）
> - Deployment 门禁验证时，优先查变更记录表，其次查条目备注

| 日期 | 版本 | CR-ID | 变更说明 | 变更人 |
|------|------|-------|---------|--------|
| YYYY-MM-DD | v1.0 | - | 初始版本 | [作者] |
| YYYY-MM-DD | vX.Y | CR-YYYYMMDD-NNN | [变更说明] | [变更人] |
