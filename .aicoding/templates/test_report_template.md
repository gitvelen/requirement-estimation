# [功能模块] 测试报告

## 文档元信息
| 项 | 值 |
|---|---|
| 状态 | Draft / Review / Approved |
| 作者 |  |
| 评审/验收 |  |
| 日期 | YYYY-MM-DD |
| 版本 | v0.1 |
| 关联需求 | `docs/<版本号>/requirements.md` |
| 关联计划 | `docs/<版本号>/plan.md` |
| 基线版本（对比口径） | tag / commit（例如 `v1.0`） |
| 包含 CR（如有） | CR-YYYYMMDD-001, CR-... |
| 代码版本 | commit / tag |

## 测试范围与环境
- 覆盖范围：REQ-xxx（列出或引用矩阵）
- 测试环境：DEV / STAGING / PROD-like
- 关键配置：...（脱敏）
- 数据准备与清理：...

## 测试分层概览
| 测试层级 | 用例数 | 通过 | 失败 | 跳过 | 覆盖说明 |
|---------|-------|------|------|------|---------|
| 单元测试 | | | | | 核心业务逻辑 |
| 集成测试 | | | | | API + 数据库 |
| E2E 测试 | | | | | 关键用户路径 |
| 回归测试 | | | | | 全量已有用例 |

## 需求覆盖矩阵（GWT 粒度追溯）

> **填写规则（🔴 MUST）**：
> - requirements.md 中出现的每条 `GWT-ID` 必须在此表中出现并给出判定，不允许遗漏
> - 门禁会从此表提取覆盖集合，与 requirements.md 的全量 `GWT-ID` 做差集验真

<!-- TEST-COVERAGE-MATRIX-BEGIN -->
| GWT-ID | REQ-ID | 需求摘要 | 对应测试(TEST-ID) | 证据类型 | 证据 | 结果 |
|--------|--------|---------|-------------------|---------|------|------|
| GWT-REQ-001-01 | REQ-001 | 用户登录 | TEST-001 | RUN_OUTPUT | pytest 输出片段/链接 | ✅ |
| GWT-REQ-C001-01 | REQ-C001 | 禁止展示内部字段 | TEST-010 | UI_PROOF | 截图链接/E2E断言输出 | ✅ |
<!-- TEST-COVERAGE-MATRIX-END -->

<!-- SKELETON-END: 以下为参考材料，按需阅读 -->

## 用例/场景测试详情

### TEST-001：正常登录
**对应需求**：REQ-001  
**对应场景**：SCN-001（如有）

**前置条件**：
- ...

**测试步骤**：
1. 输入正确账号密码
2. 点击登录

**预期结果**：进入系统首页

**实际结果**：符合预期

**证据**：命令输出/日志/截图链接（如适用）  
**结论**：通过/不通过

---

### TEST-002：密码错误
**对应需求**：REQ-002  
**对应场景**：SCN-002（如有）

**测试步骤**：
1. 输入错误密码
2. 点击登录

**预期结果**：显示“用户名或密码错误”

**实际结果**：符合预期

**证据**：...  
**结论**：通过/不通过

## CR验证证据（🔴 MUST，Deployment门禁依赖）

> **填写规则（🔴 MUST）**：
> - 每个本次上线的 CR 必须有至少一条验证证据
> - 证据可以是：测试用例结果、压测报告、人工验证记录、截图等
> - Deployment 门禁时验证每个 CR 都有对应证据

| CR-ID | 验收标准 | 证据类型 | 证据链接/说明 | 验证结论 |
|-------|---------|---------|--------------|---------|
| CR-YYYYMMDD-001 | Given-When-Then | 测试用例结果 | 见 TEST-001 | 通过 |
| CR-YYYYMMDD-002 | Given-When-Then | 人工验证记录 | 截图附件 | 通过 |

## 回滚验证（🔴 MUST，当 CR 有回滚条件/步骤或高风险项时）

> **触发条件（🔴 MUST）**：
> - 任意 CR 填写了回滚条件/步骤
> - 或任意 CR 勾选了"强制清单触发"的高风险项（API契约、数据迁移、权限/安全、兼容性、性能、不可逆配置）
>
> **填写要求（🔴 MUST）**：
> - 提供回滚可执行性的证据链接或验证记录
> - 证据类型：回滚演练记录、回滚脚本测试结果、回滚时间验证记录等

| CR-ID | 回滚条件/步骤 | 证据类型 | 证据链接/说明 | 回滚可执行性 |
|-------|-------------|---------|--------------|-------------|
| CR-YYYYMMDD-001 | 见 CR 文档 5.风险与回滚 | 回滚演练记录 | 见附件回滚演练.log | ✅ 可执行 |
| | | | | |

## 缺陷与处理
| BUG-ID | 问题描述 | 严重程度 | 状态 | 关联REQ | 修复版本 |
|---|---|---|---|---|---|
| BUG-001 |  | 高/中/低 | 已修复/未修复 | REQ-001 |  |

## 性能测试结果（如适用）
> 对应核心原则6：证据驱动、验证可复现 — 性能需基准对比

### 基准对比
| 指标 | 基线值 | 目标值 | 实测值 | 结论 |
|---|---|---|---|---|
| 响应时间（P50/P95/P99） |  |  |  |  |
| 吞吐量（QPS/TPS） |  |  |  |  |
| 并发能力 |  |  |  |  |
| 资源占用（CPU/内存/磁盘） |  |  |  |  |

### 测试环境与命令
- 测试环境配置：...
- 压测工具：...
- 测试命令（必须可复现）：
  ```bash
  ...
  ```
- 完整报告链接：...

## 测试结论
- GWT 覆盖：X/Y（覆盖率 %）
- 用例通过：X/Y
- 已知未解决问题：...
- 整体结论：通过/不通过

## 验收签署
- 验收人：
- 验收日期：YYYY-MM-DD

## 开放问题
- [ ] ...

## 变更记录
| 版本 | 日期 | 说明 | 作者 |
|---|---|---|---|
| v0.1 | YYYY-MM-DD | 初始化 |  |
