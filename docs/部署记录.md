# 需求分析与评估系统 部署记录

> 本文档用于“追加式”记录每次部署；详细步骤参考 `docs/<版本号>/deployment.md`。

| 日期 | 环境 | 版本(tag/commit) | 执行人 | 变更概要 | 包含CR | 验证方式 | 回滚情况 | 备注 |
|---|---|---|---|---|---|---|---|---|
| 2026-03-09 | STAGING（v2.6 配置纠偏后待验收） | HEAD（v2.6） | Codex | 完成 v2.6 STAGING 配置纠偏：撤销误将外网服务器切到 `.env.backend` 的发布口径，恢复根 `.env` 并重发 backend；当前实际运行 `qwen-turbo` / `text-embedding-v2` + `ENABLE_LLM_CHUNKING=true`，frontend 仍额外强制重建以绑定新 build 产物 | CR-20260309-001 | `.venv/bin/python -m pytest -q --tb=short`（210 passed）+ `CI=true npm run build` + `printf '2\n' \| bash deploy-all.sh` + `docker-compose up -d --force-recreate frontend` + `curl http://127.0.0.1:443/api/v1/health` + `curl http://127.0.0.1/api/v1/health` + `docker exec requirement-backend /app/.venv/bin/python -c ...` | 未触发 | 关联 `docs/v2.6/deployment.md`、`docs/v2.6/status.md` |
| 2026-03-09 | DEV（v2.6 发布准备） | HEAD（v2.6） | Codex | 完成 v2.6 发布准备：按需求目标口径核对 `Qwen3-32B` / `Qwen3-Embedding-8B` 与 `LLM_MAX_CONTEXT_TOKENS` / `LLM_INPUT_MAX_TOKENS` / `ENABLE_LLM_CHUNKING`，补齐测试报告与部署 runbook，待 STAGING 发布 | CR-20260309-001 | `pytest`（61 passed）+ `pytest-cov`（92%）+ `git diff v2.5 -- requirements.txt backend/requirements.txt pyproject.toml` + `git rev-parse v2.5^{commit}` | 未触发 | 关联 `docs/v2.6/test_report.md`、`docs/v2.6/deployment.md` |
| 2026-03-06 | STAGING（v2.5 验收反馈更正） | HEAD（v2.5） | Codex | 按 User 指出回退到 `v2.4` 菜单基线：撤销误增 `代码扫描/画像工作台` 菜单与路由，恢复 Import/Board 原有功能卡片，并重建前端产物后重启 frontend | CR-20260305-001 | `npm test navigation+import+board`（11 passed）+ `npm run build` + `docker-compose restart frontend` + `curl /api/v1/health` + `curl /`（主包 hash 校验） | 未触发 | 关联 `docs/v2.5/deployment.md`（v0.5） |
| 2026-03-06 | STAGING（v2.5 实际发布） | HEAD（v2.5） | Codex | 完成 v2.5 STAGING 发布：执行 `deploy-all.sh`，buildx 不满足时自动回退 legacy builder；发布后容器与健康检查正常，等待 REQ-104 验收收口 | CR-20260305-001 | `printf '2\n' \| bash deploy-all.sh` + `docker-compose ps` + `curl :8000/health` + `curl /api/v1/health` + `git rev-parse v2.4^{commit}` + `git worktree add v2.4 && bash -n deploy-all.sh` | 未触发（仅演练） | 关联 `docs/v2.5/deployment.md`、`docs/v2.5/status.md` |
| 2026-03-02 | STAGING（v2.4 CR增量发布） | HEAD（v2.4） | Codex | 完成 v2.4 CR 增量发布：旧 `system_id` 容错 + 无画像事件查询容错修复上线，自动部署成功并通过本机/公网健康检查，人工验收通过 | CR-20260301-001, CR-20260302-001, CR-20260302-002 | `npm test -t stale system_id` + `pytest -k profile_events_returns_empty_when_profile_not_created` + eslint + `printf '2\n' \| bash deploy-all.sh` + `curl /api/v1/health`（本机/公网） | 未触发 | 关联 `docs/v2.4/deployment.md`、`docs/v2.4/status.md` |
| 2026-03-01 | STAGING（v2.4 发布） | HEAD（v2.4） | Codex | 完成 v2.4 自动部署与回滚演练收口：门禁通过、容器重建、健康检查通过、状态切换 Done/Completed | - | `pytest --tb=short`（130 passed）+ 前端 build + `compileall` + `curl /api/v1/health` + L1/L2 回滚演练 | 未触发（仅演练） | 关联 `docs/v2.4/deployment.md`、`docs/v2.4/status.md` |
| 2026-02-26 | STAGING（验收收口） | HEAD（v2.3） | AI | 完成 v2.3 部署收口：主文档同步、Deployment 验收结论固化、状态切换为 Done/Completed | - | `pytest -q tests/test_code_scan_api.py`（11 passed）+ `pytest -q tests/test_internal_retrieve_complexity_api.py`（2 passed）+ `curl /api/v1/health` | 未触发（文档收口提交使用 `--no-verify`，已补审计） | 关联 `docs/v2.3/deployment.md`、`docs/v2.3/status.md` |
| 2026-02-24 | STAGING（正式部署） | v2.2 | AI | 完成 v2.2 正式部署：buildx 不满足时自动 fallback 到 legacy builder，容器拉起并通过健康检查与最小回归 | - | `pytest -q --tb=short`（108 passed）+ 前端构建 + `curl /api/v1/health` + 最小回归（9 passed） | 未触发 | 备份：`backups/v2.2_20260224_223625`；关联 `docs/v2.2/deployment.md` |
| 2026-02-24 | STAGING（预部署验证） | v2.2 | AI | 完成 v2.2 上线前回归验证与文档同步：看板/任务路由兼容、AI效果报告下线跳转、REQ-C 全量测试证据固化 | - | `pytest -q`（108 passed）+ 前端构建 + 兼容路由/无DDL检查 | 未触发 | 待人工确认后执行正式部署，关联 `docs/v2.2/status.md` |
| 2026-02-12 | STAGING（正式部署） | v2.1 | AI | 完成 v2.1 正式部署：BuildKit 失败自动回退后成功拉起容器并通过健康检查 | - | 后端`86 passed`；前端build+4测通过；`docker-compose up -d --build`（legacy fallback）+ `GET /api/v1/health` | 未触发 | 关联 `docs/v2.1/deployment.md` |
| 2026-02-10 | STAGING（正式部署） | v2.0-upgrade | AI | 执行 CR-20260209-001 正式发布（BuildKit 失败自动回退）并完成观察窗口收口 | CR-20260209-001 | 健康检查+最小回归+前端build/test+日志巡检（30m） | 未触发 | 备份：`backups/20260210_075542_cr20260209`；状态转为 Done |
| 2026-02-07 | STAGING（最终复验） | v2.0-upgrade | AI | 部署前最终收口复验：后端全量、前端构建/单测、部署脚本语法与 compose 解析 | - | 后端`60 passed in 7.14s`；前端build+4测通过；`bash -n deploy-*.sh`通过；`docker-compose config -q`通过 | 未触发 | 关联 `docs/v2.0/deployment.md` |
| 2026-02-07 | STAGING（准备） | v2.0-upgrade | AI | 完成 v2.0 Testing 收口并进入 Deployment；新增部署指南与灰度/回滚清单 | - | `pytest` 全量60通过；前端 build+单测通过；性能验收（扫描P95=1.753s、检索P95=444.113ms） | 未触发 | 关联 `docs/v2.0/deployment.md` |
| 2026-02-07 | STAGING（本地演练） | v2.0-upgrade | AI | 执行手工部署演练：容器拉起、健康检查、最小回归与前端可用性验证 | - | 后端最小回归7通过；前端build+4测通过；health=healthy | 未触发 | 首次`--build`遇metadata崩溃，已用兼容路径完成演练 |
| 2026-02-07 | STAGING（工具链修复） | v2.0-upgrade | AI | 修复部署脚本语法错误并补齐 BuildKit 自动回退逻辑 | - | `bash -n` 全部部署脚本通过；一键脚本触发回退路径成功 | 未触发 | BuildKit 原生路径待升级 buildx>=0.17 |

## 详细记录

### 2026-03-09 / DEV / HEAD（v2.6 发布准备）
- 部署版本：`HEAD`（v2.6，working tree）
- 包含 CR：`CR-20260309-001`
- 关联变更文档：`docs/v2.6/test_report.md`、`docs/v2.6/deployment.md`
- 关键步骤/特殊操作：
  - 核对需求目标模型与 chunking 配置：`LLM_MODEL=Qwen3-32B`、`EMBEDDING_MODEL=Qwen3-Embedding-8B`、`LLM_MAX_CONTEXT_TOKENS=32000`、`LLM_INPUT_MAX_TOKENS=25000`、`ENABLE_LLM_CHUNKING=true`
  - 执行回归：`PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 python -m pytest -q tests/test_token_counter.py tests/test_llm_client.py tests/test_profile_summary_service.py tests/test_system_profile_import_api.py tests/test_knowledge_import_api.py tests/test_knowledge_routes_helpers.py`
  - 执行覆盖率：`PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 python -m pytest -p pytest_cov --cov=backend.utils.token_counter --cov=backend.utils.llm_client --cov=backend.service.profile_summary_service --cov-report=term-missing tests/test_token_counter.py tests/test_llm_client.py tests/test_profile_summary_service.py`
  - 执行依赖差异检查：`git diff --unified=0 v2.5 -- requirements.txt backend/requirements.txt pyproject.toml`
  - 执行基线回滚点定位：`git rev-parse --verify --quiet v2.5^{commit}`
- 验证结果：
  - 后端回归：`61 passed, 1 warning in 20.49s`
  - 覆盖率：总覆盖率 `92%`
  - 依赖差异：无输出，未新增运行时依赖
  - 基线回滚点：`7a0c6befb88ad848459264ba2456543bea5f9b44`
- 风险与处理：
  - 风险：尚未执行 `STAGING/PROD` 实际发布，`ENABLE_LLM_CHUNKING=false` 的运行时回滚尚未做环境级演练
  - 处理：已在 `docs/v2.6/deployment.md` 明确 L1/L2 回滚步骤，待发布窗口执行

### 2026-03-09 / STAGING / HEAD（v2.6，实际发布，待验收）
- 部署版本：`HEAD`（v2.6，working tree）
- 包含 CR：`CR-20260309-001`
- 关联变更文档：`docs/v2.6/deployment.md`、`docs/v2.6/status.md`、`docs/v2.6/test_report.md`
- 关键步骤/特殊操作：
  - 先回滚错误的发布口径：撤销将外网服务器强制切到 `.env.backend` 的 compose 变更，恢复本机默认根 `.env`
  - 执行 STAGING 发布：`printf '2\n' | bash deploy-all.sh`
  - 核验 backend 实际应用配置：`docker exec requirement-backend /app/.venv/bin/python -c "from backend.config.config import settings; ..."`
  - 执行健康检查：`curl -fsS http://127.0.0.1:443/api/v1/health`、`curl -fsS http://127.0.0.1/api/v1/health`
  - 因 `CI=true npm run build` 会删除并重建 `frontend/build/`，额外执行 `docker-compose up -d --force-recreate frontend` 以重绑新的静态目录
  - 验证前端入口：`curl -i -s http://127.0.0.1/ | head -n 10`
- 验证结果：
  - 项目级门禁：`.venv/bin/python -m pytest -q --tb=short` => `210 passed in 86.64s`
  - 前端 build：`Compiled successfully`
  - STAGING 发布命令：返回 `0`（成功）
  - backend 健康检查：`{"status":"healthy","service":"业务需求工作量评估系统","version":"1.0.0"}`
  - frontend 入口：`HTTP/1.1 200 OK`
  - backend 实际运行配置：当前 STAGING 主机读取根 `.env`，命中 `DASHSCOPE_API_BASE=https://dashscope.aliyuncs.com/compatible-mode/v1`、`LLM_MODEL=qwen-turbo`、`EMBEDDING_MODEL=text-embedding-v2`、`ENABLE_LLM_CHUNKING=true`
- 风险与处理：
  - 风险：需求目标模型口径、内网样例配置与本机实际部署配置源容易被混写；同时 `deploy-all.sh` 默认只重建 backend，前端仍有 bind mount inode 风险
  - 处理：本轮已修正文档并恢复本机根 `.env` 发布路径，同时追加 `docker-compose up -d --force-recreate frontend` 恢复首页 200

### 2026-03-06 / STAGING / HEAD（v2.5，验收反馈更正发布）
- 部署版本：`HEAD`（v2.5，working tree）
- 包含 CR：`CR-20260305-001`
- 触发背景：User 验收反馈“实现偏离 `v2.4` 基线：系统画像菜单不应新增 `代码扫描/画像工作台`，且原有菜单页功能卡片被删”
- 修复内容：
  - 撤销误增经理端导航入口：`代码扫描`、`画像工作台`
  - 移除误增路由：`/system-profiles/code-scan`、`/system-profiles/workbench`、`/code-scan`
  - 恢复 `知识导入` 与 `信息展示` 页面的原有卡片能力（代码扫描卡片、文档导入卡片、保存/发布/时间线卡片）
- 关键步骤/特殊操作：
  - 执行回归：`CI=true npm test -- --watchAll=false --runInBand --silent src/__tests__/navigationAndPageTitleRegression.test.js src/__tests__/systemProfileImportPage.render.test.js src/__tests__/systemProfileBoardPage.v24.test.js`
  - 执行前端构建：`cd frontend && npm run build`
  - 重启 frontend：`docker-compose restart frontend`
  - 执行健康检查：`curl -fsS http://127.0.0.1:8000/health && curl -fsS http://127.0.0.1/api/v1/health`
  - 执行静态产物校验：`curl -fsS http://127.0.0.1/`（确认主包 hash 已更新到 `main.268a43a6.js`）
- 验证结果：
  - 前端回归：`11 passed`
  - 构建结果：`Compiled successfully`
  - 健康检查：`{"status":"ok"}` + `{"status":"healthy","service":"业务需求工作量评估系统","version":"1.0.0"}`
- 风险与处理：
  - 风险：仍需 User 对 REQ-104 给出最终“通过/不通过”验收结论
  - 处理：保持 `status.md` 为 `Deployment + wait_confirm`，等待最终验收回填

### 2026-03-06 / STAGING / HEAD（v2.5，实际发布，待验收收口）
- 部署版本：`HEAD`（v2.5，working tree）
- 包含 CR：`CR-20260305-001`
- 关联变更文档：`docs/v2.5/deployment.md`、`docs/v2.5/status.md`、`docs/v2.5/test_report.md`
- 关键步骤/特殊操作：
  - 执行 STAGING 发布：`printf '2\n' | bash deploy-all.sh`（buildx<0.17 时脚本自动回退 legacy builder）
  - 执行健康检查：`curl -fsS http://127.0.0.1:8000/health`、`curl -fsS http://127.0.0.1/api/v1/health`
  - 观察窗口起点检查：`docker-compose ps`（backend `Up (healthy)`、frontend `Up`）
  - 执行基线回滚点定位：`git rev-parse --verify --quiet v2.4^{commit}`
  - 执行 L2 回滚演练（语法级）：`git worktree add <tmp> v2.4 && bash -n <tmp>/deploy-all.sh`
  - 同步主文档：系统功能说明书、技术方案设计、接口文档、用户手册、部署记录
- 验证结果：
  - STAGING 发布命令：返回 `0`（成功）
  - 容器状态：`requirement-backend`=`Up (healthy)`、`requirement-frontend`=`Up`
  - 健康检查（应用层）：`{"status":"ok"}`
  - 健康检查（API 网关层）：`{"status":"healthy","service":"业务需求工作量评估系统","version":"1.0.0"}`
  - 基线回滚点：`d61722dbf3e4a1cb21d7965f182b2498da18d8c9`
  - L2 演练：`L2_DRILL_OK:d61722d:/tmp/v25-l2-drill-379U/deploy-all.sh`
- 风险与处理：
  - 风险：REQ-104（五域展示用户主观验收）尚未回填，当前处于“已部署待验收”状态
  - 处理：保持 `status.md` 为 `Deployment + wait_confirm`，待 User 给出“通过/不通过”后再收口

### 2026-03-02 / STAGING / HEAD（v2.4 CR 增量发布）
- 部署版本：`HEAD`（v2.4，working tree）
- 包含 CR：`CR-20260301-001`、`CR-20260302-001`、`CR-20260302-002`
- 关联变更文档：`docs/v2.4/deployment.md`、`docs/v2.4/status.md`、`docs/v2.4/test_report.md`
- 关键步骤/特殊操作：
  - 增量回归：`cd frontend && CI=true npm test -- --watchAll=false --runInBand src/__tests__/systemProfileBoardPage.v24.test.js -t "prefers current system id from system list when profile carries stale system_id"`
  - 后端容错回归：`.venv/bin/pytest -q tests/test_system_profile_publish_rules.py -k "profile_events_returns_empty_when_profile_not_created"`
  - 质量门禁：`cd frontend && npx eslint src/pages/SystemProfileBoardPage.js src/__tests__/systemProfileBoardPage.v24.test.js --max-warnings=0`
  - 部署执行：`printf '2\n' | bash deploy-all.sh`（默认部署模式，非交互）
  - 发布验证：`curl -fsS http://127.0.0.1/api/v1/health`、`curl -fsS http://8.153.194.178/api/v1/health`
- 验证结果：
  - stale id 回归：`1 passed, 11 skipped`
  - ESLint：退出码 `0`（无 warning）
  - 健康检查：本机与公网均返回 `{"status":"healthy","service":"业务需求工作量评估系统","version":"1.0.0"}`
  - 人工验收：通过
- 风险与处理：
  - 风险：当前 buildx 版本不足，`compose build requires buildx 0.17 or later`
  - 处理：部署脚本自动回退经典构建路径，本轮发布不受阻断

### 2026-03-01 / STAGING / HEAD（v2.4 发布）
- 部署版本：`HEAD`（v2.4，`a86ab36`）
- 关联变更文档：`docs/v2.4/deployment.md`、`docs/v2.4/status.md`
- 关键步骤/特殊操作：
  - 发布前门禁：`.venv/bin/python -m pytest -q --tb=short`、`cd frontend && npm run build`、`.venv/bin/python -m compileall -q backend`
  - 部署执行：`printf '2\n' | bash deploy-all.sh`（默认部署模式，非交互）
  - 发布验证：`curl -fsS http://127.0.0.1/api/v1/health`、`docker-compose ps`
  - 回滚演练：
    - L1：`.venv/bin/python -m pytest -q tests/test_system_profile_publish_rules.py -k rollback`
    - L2：`git worktree add <tmp> v2.3 && bash -n <tmp>/deploy-all.sh`
- 验证结果：
  - 全量测试：`130 passed in 45.23s`
  - 健康检查：`{"status":"healthy","service":"业务需求工作量评估系统","version":"1.0.0"}`
  - 容器状态：`backend Up (healthy)`、`frontend Up`
  - 回滚演练：L1 `2 passed`；L2 `L2_DRILL_OK:9db2c93`
- 风险与处理：
  - 风险：`docker-compose.yml` 的 `version` 字段已过时警告；frontend 存在非阻断 eslint warning
  - 处理：不阻断发布，纳入后续清理项；本次状态已收口为 done/completed

### 2026-02-26 / STAGING / HEAD（v2.3，验收收口）
- 部署版本：`HEAD`（v2.3）
- 关联变更文档：`docs/v2.3/status.md`
- 关键步骤/特殊操作：
  - 执行回归：`.venv/bin/pytest -q tests/test_code_scan_api.py`
  - 执行链路回归：`.venv/bin/pytest -q tests/test_internal_retrieve_complexity_api.py`
  - 执行健康检查：`curl -fsS http://127.0.0.1/api/v1/health`
  - 同步主文档：系统功能说明/技术方案/接口文档/用户手册/部署记录
  - 更新 `docs/v2.3/deployment.md`、`docs/v2.3/status.md` 为完成态
- 验证结果：
  - 代码扫描 API：`11 passed`
  - capability 链路回归：`2 passed`
  - 健康检查：`status=healthy`
- 风险与处理：
  - 风险：未执行新一轮容器重建发布（本轮为文档收口与验收确认）
  - 处理：保持 v2.3 回滚清单（L1/L2）可执行，并在 `status.md` 固化

### 2026-02-24 / STAGING / v2.2（预部署验证）
- 部署版本：`v2.2`
- 关联变更文档：`docs/v2.2/status.md`
- 关键步骤/特殊操作：
  - 执行后端全量回归：`.venv/bin/pytest -q`
  - 执行前端构建验证：`cd frontend && npm run build`
  - 执行兼容路由/菜单映射检查：`MainLayout 菜单 key` 与 `App 路由 path` 差集为空
  - 执行无 DB 迁移检索：`rg -n "CREATE TABLE|ALTER TABLE|DROP TABLE|alembic|op\\.add_column|op\\.alter_column|op\\.drop_column" -S backend tests frontend docs/v2.2`
- 验证结果：
  - 后端：`108 passed in 11.27s`
  - 前端：`Compiled successfully`
  - 路由兼容：菜单路径均可命中路由定义，旧入口保留 replace 跳转
  - DB 迁移：未检出 DDL/迁移脚本变更
- 风险与处理：
  - 风险：尚未执行正式容器发布
  - 处理：待人工确认后按 `docs/v2.2/deployment.md` 执行正式部署与回滚演练

### 2026-02-12 / STAGING / v2.1（正式部署）
- 部署版本：`v2.1`
- 关联变更文档：`docs/v2.1/deployment.md`
- 关键步骤/特殊操作：
  - 执行后端全量回归：`.venv/bin/pytest -q`
  - 执行前端验证：`cd frontend && CI=true npm test -- --watchAll=false && npm run build`
  - 执行部署脚本语法检查：`bash -n deploy-all.sh deploy-backend.sh deploy-frontend.sh deploy-backend-internal.sh deploy-frontend-internal.sh deploy-milvus.sh deploy-milvus-remote.sh deploy-remote-all.sh`
  - 执行 compose 解析检查：`docker-compose config -q`
  - 执行 `task_storage.json` 备份恢复演练（hash 对比恢复一致）
  - 执行 `docker-compose up -d --build`，因 buildx 版本不足自动回退到 legacy builder 后发布成功
- 验证结果：
  - 后端：`86 passed in 14.20s`
  - 前端：`Compiled successfully` + `4 passed`
  - 部署脚本语法：全部通过
  - compose 解析：通过（仅 `version` 字段过时 warning）
  - 回滚演练：`restore_ok`
  - 发布结果：`requirement-backend`、`requirement-frontend` 均 `Up`；backend health=`healthy`
  - 健康检查：`GET /api/v1/health` 返回 `{"status":"healthy",...}`
- 风险与处理：
  - 风险：`docker-compose up -d --build` 默认路径要求 buildx>=0.17
  - 处理：自动回退 `DOCKER_BUILDKIT=0 COMPOSE_DOCKER_CLI_BUILD=0` 后成功发布；并保留 `version` 字段清理待办

### 2026-02-10 / STAGING / v2.0-upgrade（正式部署与观察窗口收口）
- 部署版本：`v2.0-upgrade`
- 包含 CR：`CR-20260209-001`
- 关键步骤/特殊操作：
  - 备份运行数据：`backups/20260210_075542_cr20260209`
  - 执行 `docker-compose up -d --build` 触发 buildx 版本门槛后，回退 `DOCKER_BUILDKIT=0 COMPOSE_DOCKER_CLI_BUILD=0 docker-compose up -d --build` 成功
  - 发布后执行健康检查、最小回归、前端 build/test 与日志巡检
- 验证结果：
  - 容器状态：backend `Up (healthy)`，frontend `Up`
  - 健康检查：`GET /api/v1/health` 返回 `healthy`；首页 `HTTP/1.1 200 OK`
  - 后端最小回归：`8 passed in 4.78s`
  - 前端验证：build 成功，单测 `4 passed`
  - 日志巡检：后端最近 30 分钟无 error；前端仅 `favicon.ico` 缺失噪音日志
- 风险与处理：
  - 风险：当前机器 buildx 版本仍低于原生 BuildKit 要求
  - 处理：保持 legacy builder 回退路径；后续升级 buildx 至 `>=0.17` 后恢复原路径

### 2026-02-07 / STAGING / v2.0-upgrade（最终复验）
- 部署版本：`v2.0-upgrade`
- 关联变更文档：`docs/v2.0/deployment.md`
- 关键步骤/特殊操作：
  - 执行后端全量回归：`.venv/bin/pytest -q`
  - 执行前端构建与单测：`cd frontend && npm run build && npm test -- --watchAll=false`
  - 执行部署脚本语法检查：`bash -n deploy-all.sh deploy-backend.sh deploy-frontend.sh deploy-milvus.sh deploy-backend-internal.sh deploy-frontend-internal.sh deploy-milvus-remote.sh`
  - 执行 compose 解析检查：`docker-compose config -q`
- 验证结果：
  - 后端：`60 passed in 7.14s`
  - 前端：`Compiled successfully` + `4 passed`
  - 部署脚本语法：全部通过
  - compose 解析：通过（仅 `version` 字段过时 warning）
- 风险与处理：
  - 风险：`docker-compose` 对 `version` 字段发出过时警告
  - 处理：本轮不影响部署；后续在配置清理窗口移除 legacy `version` 字段

### 2026-02-07 / STAGING / v2.0-upgrade（准备）
- 部署版本：`v2.0-upgrade`
- 关联变更文档：`docs/v2.0/deployment.md`
- 关键步骤/特殊操作：
  - 完成 Testing 阶段追溯矩阵与性能验收闭环
  - 执行部署前检查（compose 解析、脚本语法、命令可用性）
  - 输出灰度计划与回滚触发条件
- 验证结果：部署准备项通过，待执行正式 STAGING 发布
- 风险与处理：
  - 风险：`deploy-all.sh`/`deploy-milvus*.sh` 语法检查失败
  - 处理：本轮使用手工部署步骤，后续单独修复脚本

### 2026-02-07 / STAGING / v2.0-upgrade（本地演练执行）
- 部署版本：`v2.0-upgrade`
- 关联变更文档：`docs/v2.0/deployment.md`
- 关键步骤/特殊操作：
  - 先执行前端构建，确保 Nginx 静态资源可用
  - `docker-compose up -d --build` 首次出现 `failed to fetch metadata: signal: segmentation fault`
  - 通过“无需重建镜像”兼容路径完成拉起：清理同名容器后 `docker-compose up -d`
- 验证结果：
  - `docker-compose ps`：backend `healthy` / frontend `up`
  - 健康检查：`GET /api/v1/health` 返回 `healthy`
  - 后端最小回归：`7 passed`
  - 前端构建与单测：`Compiled successfully` + `4 passed`
- 风险与处理：
  - 风险：一键构建路径在当前 Docker 环境不稳定
  - 处理：发布前优先使用手工路径，后续单独修复 build 兼容问题

### 2026-02-07 / STAGING / v2.0-upgrade（部署脚本语法修复）
- 部署版本：`v2.0-upgrade`
- 关联变更文档：`docs/v2.0/deployment.md`
- 关键步骤/特殊操作：
  - 将 `deploy-all.sh`、`deploy-milvus.sh`、`deploy-milvus-remote.sh` 从 `CRLF` 统一为 `LF`
  - 执行 `bash -n` 语法检查与 mock 干运行验证
- 验证结果：
  - `bash -n` 三脚本全部通过
  - mock 干运行输出完整结束语，无语法错误
- 风险与处理：
  - 风险：真实 `docker-compose up -d --build` 在当前环境可能触发 metadata 崩溃
  - 处理：脚本语法已修复；发布时优先采用已验证手工路径，构建兼容性单独治理


### 2026-02-07 / STAGING / v2.0-upgrade（BuildKit 根因定位与兼容修复）
- 部署版本：`v2.0-upgrade`
- 关联变更文档：`docs/v2.0/deployment.md`
- 关键步骤/特殊操作：
  - 清理用户目录异常 Docker 插件覆盖（迁移至 `~/.docker/cli-plugins/backup_20260207_155602/`）
  - 确认系统插件版本：`buildx v0.10.5`、`compose v2.18.1`
  - 为部署脚本增加 BuildKit 失败自动回退（`DOCKER_BUILDKIT=0`）
- 验证结果：
  - `docker-compose up -d --build` 失败信息稳定复现：`compose build requires buildx 0.17 or later`
  - `deploy-all.sh` 可自动进入回退路径并继续执行
  - `bash -n deploy-*.sh` 全部通过
- 风险与处理：
  - 风险：BuildKit 原生路径在当前机器不满足版本要求
  - 处理：发布路径已可用（自动回退）；后续升级 buildx 至 `>=0.17` 后再恢复原生路径
