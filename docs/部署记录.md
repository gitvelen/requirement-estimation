# 需求分析与评估系统 部署记录

> 本文档用于“追加式”记录每次部署；详细步骤参考 `docs/<版本号>/deployment.md`。

| 日期 | 环境 | 版本(tag/commit) | 执行人 | 变更概要 | 包含CR | 验证方式 | 回滚情况 | 备注 |
|---|---|---|---|---|---|---|---|---|
| 2026-02-10 | STAGING（正式部署） | v2.0-upgrade | AI | 执行 CR-20260209-001 正式发布（BuildKit 失败自动回退）并完成观察窗口收口 | CR-20260209-001 | 健康检查+最小回归+前端build/test+日志巡检（30m） | 未触发 | 备份：`backups/20260210_075542_cr20260209`；状态转为 Done |
| 2026-02-07 | STAGING（最终复验） | v2.0-upgrade | AI | 部署前最终收口复验：后端全量、前端构建/单测、部署脚本语法与 compose 解析 | - | 后端`60 passed in 7.14s`；前端build+4测通过；`bash -n deploy-*.sh`通过；`docker-compose config -q`通过 | 未触发 | 关联 `docs/v2.0/deployment.md` |
| 2026-02-07 | STAGING（准备） | v2.0-upgrade | AI | 完成 v2.0 Testing 收口并进入 Deployment；新增部署指南与灰度/回滚清单 | - | `pytest` 全量60通过；前端 build+单测通过；性能验收（扫描P95=1.753s、检索P95=444.113ms） | 未触发 | 关联 `docs/v2.0/deployment.md` |
| 2026-02-07 | STAGING（本地演练） | v2.0-upgrade | AI | 执行手工部署演练：容器拉起、健康检查、最小回归与前端可用性验证 | - | 后端最小回归7通过；前端build+4测通过；health=healthy | 未触发 | 首次`--build`遇metadata崩溃，已用兼容路径完成演练 |
| 2026-02-07 | STAGING（工具链修复） | v2.0-upgrade | AI | 修复部署脚本语法错误并补齐 BuildKit 自动回退逻辑 | - | `bash -n` 全部部署脚本通过；一键脚本触发回退路径成功 | 未触发 | BuildKit 原生路径待升级 buildx>=0.17 |

## 详细记录

### 2026-02-10 / STAGING / v2.0-upgrade（正式部署与观察窗口收口）
- 部署版本：`v2.0-upgrade`
- 包含 CR：`CR-20260209-001`
- 关键步骤/特殊操作：
  - 备份运行数据：`backups/20260210_075542_cr20260209`
  - 执行 `docker-compose up -d --build` 触发 buildx 版本门槛后，回退 `DOCKER_BUILDKIT=0 COMPOSE_DOCKER_CLI_BUILD=0 docker-compose up -d --build` 成功
  - 发布后执行健康检查、最小回归、前端 build/test 与日志巡检
- 验证结果：
  - 容器状态：backend `Up (healthy)`，frontend `Up`
  - 健康检查：`GET /api/v1/health` 返回 `healthy`；首页 `HTTP/1.1 200 OK`
  - 后端最小回归：`8 passed in 4.78s`
  - 前端验证：build 成功，单测 `4 passed`
  - 日志巡检：后端最近 30 分钟无 error；前端仅 `favicon.ico` 缺失噪音日志
- 风险与处理：
  - 风险：当前机器 buildx 版本仍低于原生 BuildKit 要求
  - 处理：保持 legacy builder 回退路径；后续升级 buildx 至 `>=0.17` 后恢复原路径

### 2026-02-07 / STAGING / v2.0-upgrade（最终复验）
- 部署版本：`v2.0-upgrade`
- 关联变更文档：`docs/v2.0/deployment.md`
- 关键步骤/特殊操作：
  - 执行后端全量回归：`.venv/bin/pytest -q`
  - 执行前端构建与单测：`cd frontend && npm run build && npm test -- --watchAll=false`
  - 执行部署脚本语法检查：`bash -n deploy-all.sh deploy-backend.sh deploy-frontend.sh deploy-milvus.sh deploy-backend-internal.sh deploy-frontend-internal.sh deploy-milvus-remote.sh`
  - 执行 compose 解析检查：`docker-compose config -q`
- 验证结果：
  - 后端：`60 passed in 7.14s`
  - 前端：`Compiled successfully` + `4 passed`
  - 部署脚本语法：全部通过
  - compose 解析：通过（仅 `version` 字段过时 warning）
- 风险与处理：
  - 风险：`docker-compose` 对 `version` 字段发出过时警告
  - 处理：本轮不影响部署；后续在配置清理窗口移除 legacy `version` 字段

### 2026-02-07 / STAGING / v2.0-upgrade（准备）
- 部署版本：`v2.0-upgrade`
- 关联变更文档：`docs/v2.0/deployment.md`
- 关键步骤/特殊操作：
  - 完成 Testing 阶段追溯矩阵与性能验收闭环
  - 执行部署前检查（compose 解析、脚本语法、命令可用性）
  - 输出灰度计划与回滚触发条件
- 验证结果：部署准备项通过，待执行正式 STAGING 发布
- 风险与处理：
  - 风险：`deploy-all.sh`/`deploy-milvus*.sh` 语法检查失败
  - 处理：本轮使用手工部署步骤，后续单独修复脚本

### 2026-02-07 / STAGING / v2.0-upgrade（本地演练执行）
- 部署版本：`v2.0-upgrade`
- 关联变更文档：`docs/v2.0/deployment.md`
- 关键步骤/特殊操作：
  - 先执行前端构建，确保 Nginx 静态资源可用
  - `docker-compose up -d --build` 首次出现 `failed to fetch metadata: signal: segmentation fault`
  - 通过“无需重建镜像”兼容路径完成拉起：清理同名容器后 `docker-compose up -d`
- 验证结果：
  - `docker-compose ps`：backend `healthy` / frontend `up`
  - 健康检查：`GET /api/v1/health` 返回 `healthy`
  - 后端最小回归：`7 passed`
  - 前端构建与单测：`Compiled successfully` + `4 passed`
- 风险与处理：
  - 风险：一键构建路径在当前 Docker 环境不稳定
  - 处理：发布前优先使用手工路径，后续单独修复 build 兼容问题

### 2026-02-07 / STAGING / v2.0-upgrade（部署脚本语法修复）
- 部署版本：`v2.0-upgrade`
- 关联变更文档：`docs/v2.0/deployment.md`
- 关键步骤/特殊操作：
  - 将 `deploy-all.sh`、`deploy-milvus.sh`、`deploy-milvus-remote.sh` 从 `CRLF` 统一为 `LF`
  - 执行 `bash -n` 语法检查与 mock 干运行验证
- 验证结果：
  - `bash -n` 三脚本全部通过
  - mock 干运行输出完整结束语，无语法错误
- 风险与处理：
  - 风险：真实 `docker-compose up -d --build` 在当前环境可能触发 metadata 崩溃
  - 处理：脚本语法已修复；发布时优先采用已验证手工路径，构建兼容性单独治理


### 2026-02-07 / STAGING / v2.0-upgrade（BuildKit 根因定位与兼容修复）
- 部署版本：`v2.0-upgrade`
- 关联变更文档：`docs/v2.0/deployment.md`
- 关键步骤/特殊操作：
  - 清理用户目录异常 Docker 插件覆盖（迁移至 `~/.docker/cli-plugins/backup_20260207_155602/`）
  - 确认系统插件版本：`buildx v0.10.5`、`compose v2.18.1`
  - 为部署脚本增加 BuildKit 失败自动回退（`DOCKER_BUILDKIT=0`）
- 验证结果：
  - `docker-compose up -d --build` 失败信息稳定复现：`compose build requires buildx 0.17 or later`
  - `deploy-all.sh` 可自动进入回退路径并继续执行
  - `bash -n deploy-*.sh` 全部通过
- 风险与处理：
  - 风险：BuildKit 原生路径在当前机器不满足版本要求
  - 处理：发布路径已可用（自动回退）；后续升级 buildx 至 `>=0.17` 后再恢复原生路径
