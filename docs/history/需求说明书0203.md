# 需求评估系统 - 需求主文档（含变更记录）

> 本文件为需求主文档。初始需求与后续变更均在此维护；每次需求变更定稿后，追加到文末，并在下表登记。

## 需求版本变更总览

| 序号 | 需求版本 | 变更日期 | 作者 | 内容简介 | 页码 |
|---:|---|---|---|---|---|
| 1 | v1.0 | 2026-01-27 | 项目组 | 初始版本：文档解析、系统识别、功能点拆分、Delphi估算、报告输出、API集成、人机协作修正、拆分与复杂度口径 | P1 |
| 2 | v2.0 | 2026-01-30 | 项目组 | 评估流程与权限重构：管理员/项目经理/专家分离，多专家多轮盲评，去除Modal编辑，增加偏离度与报告版本，范围裁剪（本地向量库等） | P3 |
| 3 | v3.0 | 2026-02-01 | 项目组 | 采购严肃模式：证据库/画像/代码扫描/ESB索引/证据等级，selected/maybe/questions 分层与阻断，区间估算+风控，专家证据在线预览 | P20 |

> 注：本表页码按固定规则计算（等宽渲染、每行110列、每页45行），由脚本 `scripts/paginate_requirements.py` 生成；后续仅追加新版本内容时，既有版本的起始页码保持不变。


---

## 需求版本 v1.0（初始需求）

### 1. 背景与目标

#### 1.1 背景（要解决的问题）

- 人工拆分效率低、耗时长
- 评估标准不统一、偏差大
- 缺乏科学的方法论支撑
- 评估结果难以追溯和复用

#### 1.2 目标（核心价值/能力）

- 自动化：自动解析需求文档、识别系统、拆分功能点
- 智能化：基于AI大模型的智能分析
- 标准化：统一拆分标准与方法论（包含COSMIC全功能点分析口径）
- 可追溯：评估过程记录与结果输出可回放
- 可集成：提供RESTful API，便于接入DevOps系统
- 人机协作：支持人工修正AI结果并形成数据闭环

#### 1.3 主要应用场景

- 需求评审阶段的工作量预估
- 项目投标时的工作量评估
- 项目管理中的资源规划
- DevOps系统的需求评估模块
- 软件开发过程的成本控制

### 2. 功能范围（按优先级）

> 说明：本版本以“自动解析 → 系统识别 → 功能点拆分 → 工作量估算 → 报告输出”为主链路；支持API集成与人机协作修正。

| 功能模块 | 功能描述 | 优先级 |
|---|---|---|
| 文档解析 | 自动解析.docx格式需求文档 | P0 |
| 系统识别 | 智能识别需求涉及的所有系统 | P0 |
| 功能点拆分 | 按系统维度拆分功能点 | P0 |
| 工作量估算 | Delphi专家评估法 | P0 |
| COSMIC功能点 | 基于数据移动的功能点分析 | P1 |
| 报告生成 | 生成Excel评估报告 | P0 |
| Web界面 | 文件上传、进度查看、报告下载 | P1 |
| API接口 | RESTful API接口 | P0 |
| 知识库 | 基于Milvus的系统知识库 | P1 |
| 人机协作 | 人工修正AI评估结果 | P1 |
| 配置管理 | 子系统、主系统、COSMIC规则配置 | P2 |

### 3. 核心业务流程

```
1. 用户上传需求文档（.docx格式）
   ↓
2. 系统自动解析"需求内容说明"章节
   ↓
3. 知识库检索相似系统案例（可选）
   ↓
4. 系统识别Agent识别涉及系统
   ↓
5. 功能点拆分Agent按系统拆分功能点
   ↓
6. 工作量估算Agent进行Delphi估算
   ↓
7. 报告生成Agent生成Excel报告
   ↓
8. 【新功能】人工查看和修正评估结果
   ↓
9. 确认后生成最终报告
   ↓
10. 修正数据持久化，优化知识库
```

### 4. 人机协作（修正与闭环）

系统需支持对AI评估结果进行人工复核与修正，形成可追溯的修改记录，并支持重新生成报告：
- 查看AI评估结果（按系统分类）
- 新增/修改/删除功能点（业务描述、复杂度、人天等）
- 记录修改历史（可回放）
- 确认后重新生成最终报告
- 修正数据可用于后续评估优化（数据闭环）

### 5. 功能点拆分规则（用于“拆分Agent”输出约束）

**拆分原则**：
- **系统维度优先**：按涉及系统进行一级拆分
- **功能模块为辅**：系统内按功能模块进行二级拆分
- **粒度适中**：单个功能点0.5-5人天
- **独立可估算**：每个功能点可独立估算工作量
- **依赖清晰**：明确标注功能点间的依赖关系

**复杂度评估口径**：
- 高：涉及多系统集成；复杂算法或业务逻辑；核心业务功能；高风险或新技术
- 中：标准业务逻辑；一定复杂度；常规功能
- 低：简单增删改查；配置管理；查询展示

> 复杂度用于估算与复核提示，不应改变功能范围边界。

### 6. Delphi专家评估法（用于“工作量估算”）

**方法说明**：通过多轮匿名调查与反馈，使专家意见逐步收敛，取加权平均作为最终估算。

**实施步骤**：
- **第一轮**：5位专家独立估算每个功能点的工作量
- **反馈**：展示统计结果（最大值、最小值、平均值、标准差）
- **第二轮**：专家参考反馈，调整自己的估算
- **第三轮**：重复反馈-调整过程（通常2-3轮）
- **收敛**：取加权平均值作为最终估算

**专家权重配置**：
- 核心专家（专家1-3）：权重1.0
- 普通专家（专家4-5）：权重0.8
- 权重根据专家经验和历史准确度确定

---

## ==================== 需求版本 v2.0（2026-01-30） ====================

### 需求评估系统 - 需求变更说明书

#### 文档信息

| 项目 | 内容 |
|------|------|
| 文档版本 | v2.0 |
| 编写日期 | 2026-01-30 |
| 变更类型 | 系统升级（范围裁剪说明） |

---

#### 1. 概述

本次系统升级主要针对用户交互界面和评估流程进行全面优化，解决现有系统中"在线编辑评估结果"功能不合理的问题，并引入多专家协作评估机制。

##### 核心变更点

1. **去除Modal弹窗编辑**：专家评估时无需点击"编辑"按钮，改为表格内直接点击编辑
2. **引入多专家协作**：支持3位专家独立盲评，计算AI预估与专家均值的偏离度
3. **角色权限分离**：明确管理员、项目经理、专家三种角色的权限边界
4. **多轮评估机制**：偏离度超过20%时自动触发下一轮评估，最多3轮

##### 本期范围与非目标（2026-01-30）

**本期重点**：优先保证“知识库可用”，用于增强系统识别/功能拆分等Agent能力（导入、检索、统计/指标）。  
**实现方式**：本期不启用 MinIO/Milvus，知识库使用本地文件向量库（`data/knowledge_store.json`）。
**本期明确不做（延期到下一期）**：

1. K8s 部署与 CI/CD 流水线
2. 数据库持久化（PostgreSQL/迁移/索引/相关表）
3. Redis 缓存
4. MinIO（包含业务文件存储与Milvus依赖组件）
5. Milvus 向量数据库（服务化部署）
6. 生产化中间件（统一异常、请求日志、限流、链路追踪等）

##### 风险提示（由范围裁剪带来）

- 继续采用文件存储（`data/*.json`）会带来并发写入与一致性风险；多实例扩容时需要额外方案避免数据分叉/覆盖。
- 未引入 Redis/数据库会限制性能与可观测性，数据规模上来后会出现明显瓶颈（列表/统计/未读数等）。
- 暂不做生产化中间件会增加排障成本，接口错误表现与日志颗粒度在高并发下可能不足。
- 知识库采用本地向量库属于单机方案，检索为全量扫描，数据量上来后需要迁移到 Milvus（下一期）。

---

#### 2. 角色与权限

##### 2.1 角色定义

| 角色 | 说明 | 兼职规则 |
|------|------|---------|
| **管理员** | 负责系统配置、用户管理、查看所有任务 | 不可兼职其他角色，全局唯一 |
| **项目经理** | 发起评估任务、编辑功能点信息、查看报告 | 可兼任专家 |
| **专家** | 对功能点进行人天评估，参与多轮评估 | 可兼任项目经理 |

##### 2.2 权限矩阵

| 功能模块 | 管理员 | 项目经理 | 专家 |
|---------|-------|---------|-----|
| 查看所有任务 | ✅ | ❌ | ❌ |
| 查看自己发起的任务 | ✅ | ✅ | ❌ |
| 查看自己参与的评估 | ❌ | ❌ | ✅ |
| 发起评估任务 | ❌ | ✅ | ❌ |
| 分配专家 | ✅ | ❌ | ❌ |
| 编辑功能点信息 | ❌ | ✅ | ❌ |
| 评估人天数 | ❌ | ❌ | ✅ |
| 查看最终报告 | ✅ | ✅ | ❌ |
| 系统清单（主系统清单+子系统映射） | ✅ | ❌ | ❌ |
| 规则管理（估算规则） | ✅ | ❌ | ❌ |
| 用户管理（含角色/专家信息） | ✅ | ❌ | ❌ |
| 知识库管理 | ❌ | ✅ | ❌ |
| AI效果报告 | ✅ | ✅ | ✅ |

---

#### 3. 任务状态流转

##### 3.1 状态定义

| 状态 | 说明 | 可见操作 |
|------|------|---------|
| **草稿** | AI评估完成后，项目经理可编辑 | 提交给管理员、删除、编辑功能点 |
| **待分配** | 已提交给管理员，等待分配专家 | 无（管理员操作） |
| **评估中** | 专家正在评估，显示X/3进度 | 查看详情、下载报告（完成后） |
| **已完成** | 评估完成，报告已生成 | 查看详情、下载报告、归档 |
| **已归档** | 任务归档，不可再编辑 | 查看详情、下载报告 |

##### 3.2 流程图

```
项目经理上传文档
       ↓
   AI评估中...
       ↓
   AI评估完成（草稿）
       ↓
项目经理提交给管理员
       ↓
  待分配（角标提示管理员）
       ↓
管理员分配3位专家
       ↓
   评估中（1/3 → 2/3 → 3/3）
       ↓
   3位全部提交
       ↓
   计算偏离度
       ↓
有偏离度>20%？
    ↙       ↘
  是          否
   ↓           ↓
第2轮评估    生成报告版本（PDF）
   ↓
（3位全部提交）
   ↓
   计算偏离度
   ↓
仍有>20%？
    ↙       ↘
  是          否
   ↓           ↓
第3轮评估    生成报告版本（PDF）
   ↓
   生成报告版本（PDF）
```

---

#### 4. 专家评估流程

##### 4.1 入口方式

1. **邀请链接**：管理员复制邀请链接发送给专家
2. **登录识别**：专家点击链接后，通过用户名密码登录，系统根据邀请Token识别身份
3. **直接进入**：登录成功后直接跳转到评估页面

##### 4.2 评估页面

###### 表格列结构

| 序号 | 功能模块 | 功能点 | 业务描述 | 输入 | 输出 | 依赖项 | 预估人天数 | 备注 |
|------|---------|--------|---------|------|------|--------|-----------|------|

###### 交互规则

- **灰色数值** = AI预估（默认值）
- **黑色高亮 + 淡蓝背景** = 专家修改后的值
- 点击"预估人天数"单元格直接进入编辑状态
- 失焦自动保存
- 未编辑的值，提交时默认采用AI预估值

###### 顶部提示

> "灰色数值为AI预估，点击可修改。未修改的值将以AI预估作为您的评估结果。"

###### 多系统Tab

AI识别后会自动创建系统Tab（如：核心系统、渠道系统），专家可通过顶部Tab切换。

###### 高偏离功能点（第二/三轮）

- **仅展示高偏离功能点**（>20%），其他功能点不进入本轮
- 高偏离功能点行用红色高亮显示
- 顶部Alert提示本轮有多少高偏离功能点需要重点评估
- **不展示上一轮专家自己的值**，避免锚定效应（仅显示AI预估值）

###### 底部操作栏

- **提交评估**：确认完成本轮评估，提交后可撤回（仅限未生成报告）
- **返回列表**：返回专家任务列表

##### 4.3 提交后处理

- 弹窗提示"评估已提交"
- 3秒后自动跳转到专家任务列表
- 已提交的任务显示"查看"按钮，可查看但不可编辑
- **撤回规则**：仅允许在**未生成报告**前撤回；撤回后需**回滚偏离度与任务状态/轮次进度**

---

#### 5. 偏离度计算

##### 5.1 计算公式

```
偏离度 = |3位专家均值 - AI预估| / AI预估 × 100%
```

##### 5.2 展示规则

- **所有角色可见**偏离度数据
- **报告格式**：PDF
- **版本管理**：每轮评估完成后生成一份PDF报告版本（可追溯与下载）
- **最终报告**中新增"偏离度"列，每个功能点都显示偏离度
- **高偏离阈值**：20%
- 偏离度 > 20% 的功能点，在第二/三轮评估时红色高亮

---

#### 6. 页面结构

##### 6.1 整体布局

- **左侧边栏**：按角色分组的导航菜单
- **顶部Header**：用户信息、通知入口、退出登录
- **主内容区**：页面内容

##### 6.2 侧边栏菜单结构

###### 管理员

```
任务管理（在途中/已完成）
配置管理
  ├── 系统清单
  ├── 规则管理
  └── 用户管理
效果统计（AI效果报告）
个人
  ├── 消息通知
  └── 个人中心
```

###### 项目经理

```
任务管理（我发起的）
配置管理
  └── 知识库管理
效果统计（AI效果报告）
个人
  ├── 消息通知
  └── 个人中心
```

###### 专家

```
任务管理（我参与的）
效果统计（AI效果报告）
个人
  ├── 消息通知
  └── 个人中心
```

###### 项目经理 + 专家

```
任务管理（我发起的 / 我参与的）
配置管理
  └── 知识库管理
效果统计（AI效果报告）
个人
  ├── 消息通知
  └── 个人中心
```

---

#### 7. 各页面详细需求

##### 7.1 登录页

- **布局**：全屏背景 + 居中半透明登录框
- **表单**：用户名、密码、登录按钮
- **登录时不设定角色**，角色由后台配置

##### 7.2 任务列表页

###### 列表字段

| 字段 | 说明 |
|------|------|
| 任务名称 | 评估任务的名称 |
| 发起人 | 项目经理姓名 |
| 创建时间 | 任务创建时间 |
| 状态 | 草稿/待分配/评估中/已完成/已归档 |
| 当前进度 | 评估中时显示X/3 |
| 当前轮次 | 1/2/3 |
| 操作 | 查看详情、删除（草稿/待分配）、下载报告（按轮次版本） |

###### 筛选功能

- 状态筛选
- 日期范围筛选

###### 管理员视图分组与排序

- **在途中**：草稿 / 待分配 / 评估中
- **已完成**：已完成 / 已归档
- 默认按**创建时间倒序**展示（最近优先）
- 仍保留状态筛选作为兜底

##### 7.3 任务详情页

###### 布局

- **左侧**：基本信息卡片
  - 任务名称、发起人、创建时间、状态、当前轮次、涉及系统
  - 需求文档下载
- **右侧**：评估进度 + 操作区
  - 3位专家的提交状态
  - 邀请链接（仅管理员可见）
  - 偏离度统计（预览/正式）
  - **报告版本列表**（每轮PDF，可下载/追溯）

###### 评估进度展示

```
专家A：已提交 2025-01-28 10:00
专家B：已提交 2025-01-28 11:00
专家C：未提交
```

###### 偏离度统计

- 所有专家提交后显示**预览**
- 每轮报告生成后显示**正式数据**
- 包含：整体偏离度、高偏离功能点列表

##### 7.4 专家评估页

详见"四、专家评估流程"

##### 7.5 配置管理页

###### 系统清单（管理员）

- Tab：主系统清单（系统名称/简称/状态，支持增删改与热加载）
- Tab：子系统映射（子系统名称 → 所属主系统简称，支持增删改与热加载）
- 支持通过模板 XLSX 批量导入铺底数据（覆盖/增量导入）

###### 规则管理（估算规则，管理员）

- 系统选择
- 功能模块
- 复杂度等级
- 基准人天
- 描述

###### 用户管理（管理员）

- 维护用户与角色权限（管理员/项目经理/专家）
- **专家信息并入用户管理**：当用户包含 expert 角色时，需维护部门/专长/在岗状态

##### 7.6 知识库文档管理

###### 布局

- 顶部：**主系统选择器**（一个主系统一套知识库）
- 统计卡片：当前主系统知识总数、检索命中率、平均相似度等
- 导入区：上传区域（DOCX / PPTX）+ 模板下载（DOCX / PPTX）
- 检索区：仅在当前主系统内检索（用于验证导入效果）

###### 本期导入策略（无DB/Redis/MinIO/Milvus）

- 仅维护 **system_profile（系统知识）**
- 导入格式：DOCX / PPTX（不提供 CSV 模板）
- 导入策略：AI 结构化抽取为主；抽取失败则提示失败并要求按模板补全后重试
- 数据落地：本地文件向量库 `data/knowledge_store.json`

###### system_profile 字段（用于系统校准）

> 说明：知识库不维护“估算案例”，只维护系统画像，用于提升系统识别与功能点归属准确性；人天仅作参考值。

- 基础字段：system_name、system_short_name、system_category、business_goal、core_functions、tech_stack、architecture、performance、main_users、notes
- 【新增】校准字段（强烈建议填写）：
  - in_scope：系统职责/边界（做什么）
  - out_of_scope：明确不做什么（用于避免误拆）
  - integration_points：主要集成点/上下游（用顿号分隔）
  - key_constraints：关键约束（合规/性能/数据敏感/发布窗口等）

###### 权限

- **项目经理**：可上传自己负责主系统的文档（按主系统维度维护）

##### 7.7 AI效果报告页

###### 布局

- **左侧**：维度导航
  - 系统对比
  - 类型分析（功能模块维度）
  - 时间趋势
  - 项目经理维度

###### 专家视图限制

- 默认仅展示**我参与的任务**的指标
- 仅展示**汇总匿名指标**（不展示项目经理维度）
- **右侧**：图表展示区

###### 统计维度与筛选

- **时间维度**：以“报告版本生成时间”为时间轴展示趋势
- **系统维度**：按任务中的系统Tab统计
- **类型维度**：按功能模块（Feature.module）统计
- **项目经理维度**：按任务创建者统计
- 支持多维度组合筛选（系统 × 类型 × 时间 × 项目经理）

###### 指标

| 指标 | 说明 |
|------|------|
| 人天准确率 | 1 - 被修改的人天数占比 |
| 功能点保留率 | 未被删除的功能点占比 |
| 字段修改率 | 业务描述/输入/输出/依赖项被修改的占比 |
| 知识库命中率 | AI评估时引用知识库的次数占比 |
| 系统识别准确率 | 未被删除的系统Tab数 / AI识别的系统数 |
| 新增功能点占比 | 项目经理新增功能点数 / AI识别功能点数 |
| 项目经理信任度 | 哪些项目经理修改最少/最多 |

###### 数据采集要求

- 记录**AI识别的系统Tab数**、系统Tab被删除/重命名的情况
- 记录功能点**新增/删除/字段修改**（描述、输入、输出、依赖项、人天）
- 记录**知识库检索命中与引用次数**（按任务/功能点/知识类型）
- 每轮评估完成后生成**指标快照**，用于时间趋势与版本追溯

##### 7.8 用户管理页

###### 列表字段

| 字段 | 说明 |
|------|------|
| 用户名 | 登录用户名 |
| 姓名 | 真实姓名 |
| 角色 | 管理员/项目经理/专家（可多选） |
| 邮箱 | 联系邮箱 |
| 手机 | 联系电话 |
| 所属部门 | 组织架构 |
| 专长领域 | 专家可见：功能模块 |
| 历史评估数 | 专家可见 |
| 发起任务数 | 项目经理可见 |
| 状态 | 启用/禁用 |
| 操作 | 编辑、禁用/启用 |

###### 批量导入流程

1. 点击"批量导入"按钮
2. 下载Excel模板
3. 填写用户信息
4. 上传文件
5. 系统解析，展示预览表格（可编辑）
6. 确认后批量创建用户

###### Excel模板格式

| 用户名 | 姓名 | 密码 | 邮箱 | 手机 | 所属部门 | 角色 | 专长领域1 | 专长领域2 | 专长领域3 | 专长领域4 | 专长领域5 |
|--------|------|------|------|------|----------|------|-----------|-----------|-----------|-----------|-----------|
| expert1 | 专家A | 123456 | a@b.com | 138xxx | 技术部 | 专家 | 账户管理 | 支付结算 | | | |

##### 7.9 个人中心页

###### 布局

- **左侧**：个人信息卡片
  - 头像
  - 用户名
  - 姓名
  - 邮箱
  - 手机
  - 所属部门
  - 角色
  - 最近登录时间
- **右侧**：
  - 以“菜单/折叠面板”展示分类信息（默认展开“基本信息”）
    - 基本信息（资料编辑）
    - 密码安全（修改密码）
    - 操作记录（登录历史、密码修改、评估提交记录）

---

#### 8. 功能点编辑规则（项目经理）

##### 8.1 可编辑字段

- 功能模块
- 功能点
- 业务描述
- 输入
- 输出
- 依赖项
- 系统Tab操作：新增（默认自动拆分）、重命名、删除
- 系统Tab操作：重新拆分当前系统（覆盖该系统下全部功能点，失败则保留原结果）

##### 8.2 展示规则（项目经理）

- 功能点表格展示应**完整展示内容**（支持换行），避免单行省略号影响可读性。

##### 8.3 备注规则

> 说明：AI生成的功能点会在“备注”中带出系统校准信息，便于快速复核（归属依据/系统约束/集成点/知识引用/待确认）。

| 操作 | 备注内容 |
|------|---------|
| 项目经理新增功能点 | "项目组手工添加该功能点" |
| 项目经理修改业务描述等字段 | "项目组手工修改了业务描述等字段" |
| 项目经理调整系统名称 | "项目组手工修改了系统名称" |
| 项目经理删除系统Tab | "项目组删除了该系统" |
| 项目经理新增系统Tab（自动拆分） | "项目组新增系统Tab并触发自动拆分" |

##### 8.4 AI评估失败处理

- 弹窗提示"文档解析失败，请检查格式后重新上传"
- 任务保留在草稿状态
- 提供"重新评估"按钮
- 显示错误日志

---

#### 9. 消息通知

##### 9.1 通知场景

| 场景 | 接收人 | 内容 |
|------|--------|------|
| 任务待分配 | 管理员 | "新任务需要分配专家" |
| 专家邀请 | 专家 | "您有新的评估任务" |
| 下一轮评估 | 专家 | "进入第N轮评估" |
| 报告生成 | 项目经理、管理员 | "评估报告已生成" |

##### 9.2 通知方式

- **站内通知**：侧边栏"消息通知"菜单，未读数量角标
- **独立页面**：点击进入通知列表页面

##### 9.3 通知列表

- 时间、通知类型图标、标题、内容摘要、操作按钮
- 支持：全部标记已读、清空已读、删除单个通知

---

#### 10. 数据结构说明

##### 10.1 核心实体

###### Task（任务）

```typescript
{
  id: string;
  name: string;
  description: string;
  status: 'draft' | 'awaiting_assignment' | 'evaluating' | 'completed' | 'archived';
  currentRound: number;      // 当前评估轮次 (1-3)
  maxRounds: number;         // 最大轮次，默认3
  creatorId: string;
  creatorName: string;
  createdAt: string;
  aiStatus: 'pending' | 'processing' | 'success' | 'failed';
  documentName: string;
  systems: string[];         // 涉及的系统名称列表
  expertAssignments: ExpertAssignment[];
  evaluationProgress: EvaluationProgress;
  statistics?: TaskStatistics;
}
```

###### Feature（功能点）

```typescript
{
  id: string;
  sequence: string;          // 序号，如 "1.1"
  system: string;            // 所属系统
  module: string;            // 功能模块
  name: string;              // 功能点名称
  description: string;       // 业务描述
  inputs: string[];          // 输入
  outputs: string[];         // 输出
  dependencies: string[];    // 依赖项
  aiEstimatedDays: number;   // AI预估人天
  source: 'ai' | 'manual';   // 生成方式
  expertEvaluations: ExpertEvaluation[];
  remark?: string;           // 备注信息
  isModifiedByManager: boolean;
  modifiedFields?: string[];
}
```

###### ReportVersion（报告版本）

```typescript
{
  id: string;
  taskId: string;
  round: number;             // 第几轮
  version: number;           // 版本号（同轮次递增）
  fileName: string;
  fileUrl: string;
  generatedBy: string;       // 生成者（系统/管理员）
  generatedAt: string;
}
```

###### AIEffectSnapshot（AI效果指标快照）

```typescript
{
  id: string;
  taskId: string;
  round: number;
  system?: string;
  module?: string;           // 功能模块
  managerId: string;
  metrics: Record<string, number>;
  createdAt: string;         // 报告版本生成时间
}
```

###### User（用户）

```typescript
{
  id: string;
  username: string;
  displayName: string;
  email?: string;
  phone?: string;
  department?: string;
  avatar?: string;
  roles: Role[];             // 'admin' | 'manager' | 'expert'
  isActive: boolean;
  expertise?: string[];      // 专长领域（功能模块）
  lastLoginAt?: string;
  createdAt: string;
}
```

---

#### 11. API接口说明

详细API接口设计请参见 `design.md` 文档。

---

#### 12. 视觉风格规范

##### 12.1 整体风格

- **风格**：专业商务
- **背景色**：灰白色调
- **主色调**：蓝色系（#1890ff）
- **线条**：分明

##### 12.2 字体

- **标题**：黑体
- **正文**：微软雅黑 / 苹方
- **数字**：等宽字体

##### 12.3 间距

- **基准单位**：4px
- **常用间距**：8px、16px、24px、32px

##### 12.4 圆角

- **卡片和按钮**：统一使用4px小圆角

##### 12.5 表格列显示控制

- **核心列**（固定显示）：序号、功能模块、功能点、预估人天数、操作
- **可折叠列**：业务描述、输入、输出、依赖项、备注
- **折叠控制**：表格右上角"列设置"按钮，勾选显示/隐藏

---

#### 13. 异常处理

##### 13.1 网络故障

- 保存失败时，数据回滚到上次保存的值
- 显示错误提示

##### 13.2 并发编辑

- 每位专家有独立的评估列，不存在冲突

##### 13.3 任务删除

- 评估中的任务不能删除，只能归档
- 草稿和待分配状态可删除

##### 13.4 邀请链接

- 链接在**当前分配有效**，直到任务被删除、专家被撤销或被重新分配
- **重新分配**或**重发邀请**时，旧Token立即失效
- 支持**撤销邀请**与**重发邀请**操作

##### 13.5 超时处理

- 不设超时，任务一直有效直到管理员手动归档

---

#### 14. 变更总结

| 模块 | 变更前 | 变更后 |
|------|--------|--------|
| 评估方式 | Modal弹窗编辑 | 表格内点击即编辑 |
| 专家数量 | 1位 | 3位独立盲评 |
| 人天展示 | AI预估人天（可编辑） | 预估人天数（灰色AI值，黑色专家修改值） |
| 复杂度 | 有复杂度列 | 去除复杂度列 |
| 角色 | 管理员、普通用户 | 管理员、项目经理、专家 |
| 任务发起 | 管理员上传并分配 | 项目经理上传 → 管理员分配 |
| 报告内容 | AI预估与专家对比 | AI预估、3位专家、专家均值、偏离度 |
| 评估轮次 | 单轮 | 最多3轮（偏离度>20%触发） |
| COSMIC字段 | 无 | 输入、输出、依赖项 |
| 备注 | 无 | 备注列，记录人工修改痕迹 |

---

#### 15. 下一期：完整实现（提示）

为降低本期交付风险并支持规模化使用，建议下一期补齐以下能力：

1. 数据库持久化：PostgreSQL 表结构/迁移/索引；任务、评估、报告版本、通知、操作记录等落库
2. Redis：缓存与并发控制（任务状态、未读数、热点统计等）
3. MinIO 业务文件存储：需求文档/报告/附件统一对象存储
4. 生产化中间件：统一异常、结构化日志、审计、限流、Tracing
5. K8s + CI/CD：镜像构建、自动化测试、灰度发布与回滚
6. 知识库增强：Milvus 形态启用与文档管理接口（列表/删除/权限）、结构化抽取质量提升（PDF/PPTX）、批量导入异步化与索引策略

---

## ==================== 需求版本 v3.0（2026-02-01） ====================

### 需求评估系统升级工程需求文档（V3）

> 文档版本：v3.0  
> 编写日期：2026-02-01  
> 本期目标：在“用于采购、必须严肃可审计”的前提下，优先提升 **系统识别准确性** 与 **功能拆分合理性**，并控制两类采购风险：**功能遗漏（低估）** 与 **功能夸大（高估）**（默认更严防止范围扩张）。

---

#### 0. 术语与约定

- **系统台账/系统清单**：企业已有的系统列表（名称为主，模块/功能清单缺失或不准）。
- **系统标识（system_id）**：系统简称/编码（如 `SOFP`），用于跨数据源对齐；系统名称（system_name）用于展示。本需求不规定数据库主键设计。
- **证据库（A层）**：非结构化材料（DOCX/PPTX/PDF）切块后的可检索证据集合，用于支撑系统识别/拆分的可追溯依据。
- **系统画像（B层）**：围绕“业务能力边界”的结构化摘要，可人工确认，作为更稳定的约束输入；每个字段需可回链证据依据。
- **能力目录/功能清单（事实层）**：由代码扫描产出，用于召回与校验；不得自动扩大需求范围。
- **证据等级（evidence_level）**：系统/功能点在本次任务中可用证据的综合等级（E0~E3），用于决定是否允许进入 `selected`、是否触发严肃模式阻断。
- **selected/maybe/questions**：系统识别分层输出：确定系统/不确定系统/最小澄清问题集。
- **严肃模式（阻断）**：证据不足或冲突时禁止输出“可直接用于采购”的确定结论，仅允许输出 `maybe + questions` 并提示需人工确认。
- **未接入系统（un-onboarded）**：缺少代码与材料等关键证据的系统；严肃模式下只能输出 `maybe + questions`，不得形成可用于采购的确定结论。

##### 0.1 默认参数（可配置，本期默认值）

| 参数 | 默认值 | 说明 |
|---|---:|---|
| 严肃模式 | 开启 | 默认启用阻断规则 |
| A层证据检索 Top-K | 5 | 用于系统识别/拆分注入的证据块数量 |
| A层证据检索阈值 | 0.60 | 低于阈值视为"命中不足"；相似度算法：余弦相似度 |
| ESB检索 Top-K | 8 | 用于集成点摘要/澄清问题候选 |
| ESB检索阈值 | 0.55 | 场景/服务名更短，阈值略放宽 |
| ESB检索范围 | 提供方+调用方 | 默认同时按“提供方/调用方”两种视角命中；可切换为仅提供方或仅调用方（可配置） |
| ESB有效状态 | 正常使用 | 默认仅“有效状态”参与检索/注入/统计；其它状态（如“废弃使用”）仅留痕不默认参与 |
| ESB包含废弃条目 | false | `include_deprecated=false` 时，废弃条目不进入检索与统计口径（可配置） |
| questions 数量 | 3~8 | 超出则按优先级截断（Top-N） |
| assumptions 阈值 | 4 | `assumptions_count >= 4` 触发采购风险提示/低置信度 |
| selected_systems 最大数 | 10 | 防止输出过长影响审阅效率 |
| maybe_systems 最大数 | 8 | 超出则保留更高证据/更高召回价值者 |

> 推荐排序分数（用于相似度接近时的“可信/新鲜”加权）：`rank_score = similarity × trust_weight × recency_weight`。  
> `trust_weight` 默认：高=1.00，中=0.85，低=0.70；`recency_weight`：≤180天=1.00，181~365天=0.90，366~730天=0.80，>730天=0.70，缺失日期=0.75。

---

#### 1. 背景与目标

##### 1.1 背景

现有评估链路中，AI 需要先做“系统识别 + 功能拆分”，专家可能直接参考或复用 AI 结果，且评估结果用于采购决策。因此：
- **功能遗漏**会导致漏估（采购风险）。
- **功能夸大/擅自扩范围**会导致高估（更严重的采购风险）。

同时，实际数据源现状为：
- 台账/材料无法稳定提供可靠功能清单；
- 需求文本通常缺少接口码/服务名等技术标识；
- ESB 接口清单只覆盖集成点；
- 代码（Spring Boot/Java）是“功能事实层”的可行依据。

##### 1.2 本期目标（P0）

1. **系统识别更准且可审计**：输出 `selected/maybe/questions`，每个结论可回放证据。
2. **功能拆分更合理且不扩范围**：拆分遵循需求，不擅自新增；每个功能点带证据标签与待确认项。
3. **输出可用于采购的风控形态**：人天输出“区间+置信度+假设”，低置信度/假设过多时触发阻断提示。
4. **形成可持续校准闭环**：记录专家修正，沉淀黄金数据集的最小结构。

##### 1.3 非目标（本期不做）

- 不做端到端“自动采购定价/自动招采报价”。
- 不做模型训练/微调、也不引入复杂中台（K8s/CI-CD/全链路可观测性等）。
- 不做全量代码语义理解；代码侧仅做“入口/依赖/能力目录”的轻量事实抽取。
- 不强制支持“扫描版 PDF 的 OCR”（本期仅保证可提取文本的 PDF）。

---

#### 2. 角色与权限（沿用现有系统）

| 角色 | 本期新增/调整的能力 |
|---|---|
| 项目经理（manager） | 维护证据库/画像/能力目录（试点），发起评估任务并查看系统识别/拆分的证据与风险提示；导入时审核证据材料与能力目录 |
| 管理员（admin） | 仅配置与用户/系统清单管理；证据库权限可选开放（默认关闭，可通过配置开启） |
| 专家（expert） | 不直接维护知识库；在评估页可看到"证据摘要/置信度/假设"，并可**在线预览证据原文全文（DOCX/PPTX/PDF，仅在线预览，不提供下载）**；访问范围仅限其参与任务关联的证据材料 |

---

#### 3. 总体流程（V4升级后的主链路）

```
系统台账 + 证据库(A) + 系统画像(B) + 能力目录(代码扫描) + ESB(集成点)
        ↓（证据检索 + 候选系统榜单）
系统识别（LLM 主导 + 证据约束 + 严肃模式阻断）
        ↓（按系统）
功能拆分（类型化/模板化 + 证据标签 + 归属复核）
        ↓
区间估算（置信度 + 假设 + 风险因子 + 可审计理由）
        ↓
专家评估与修正（记录为黄金数据）
        ↓
持续校准（证据/画像/模板/阈值）
```

---

#### 4. 功能需求（可验收）

> 本章节按“功能模块 → 需求点 → 验收标准”描述。编号用于研发拆分与测试追踪。

##### 4.1 证据库（A层）：非结构化材料入库与检索（FR-A）

###### FR-A1 证据材料导入

**描述**：项目经理可为某个系统导入 DOCX/PPTX/PDF 材料，系统将其解析为文本并切块入库，保存为“证据块”。  

**最小元数据（必填/可选）**：
- 必填：`system_name`、`doc_type`、`trust_level(高/中/低)`
- 可选：`doc_date`、`source_org`、`version_hint`

**验收标准**：
1. 仅允许 `manager` 导入；无权限返回 403。
2. 导入后可查询到证据块数量增加，且每条证据块包含 `system_name/source_file/chunk_id` 等可回放字段。
3. 对于无法抽取文本的文件（如扫描 PDF），导入应失败并给出可理解错误（而不是“导入成功但无内容”）。

###### FR-A2 证据检索

**描述**：输入查询文本（例如需求片段），可按 `system_name` 过滤检索证据块，返回 Top-K 证据及相似度。  

**验收标准**：
1. 检索结果每条包含：`similarity`、`chunk_text`（可截断）、`source_file`、`doc_date/trust_level`（如有）。
2. 支持 `system_name` 过滤与不指定系统的全局检索两种模式。
3. 默认 `top_k=5`、`similarity_threshold=0.60`（可配置）。
4. 相似度算法：余弦相似度（由向量数据库/检索引擎决定）。
5. 对同一查询，返回排序需受 `trust_level + doc_date` 加权影响（相似度接近时优先更可信/更新材料；默认加权见"0.1 默认参数"）。

###### FR-A3 证据可回放

**描述**：在系统识别/功能拆分结果中，每个结论需携带“最小证据包”。  

**验收标准**：
1. 在任务详情/编辑页可查看每个 `selected_system` 的证据摘要列表（至少 1 条）。
2. 每条证据可定位到原文件（文件名+页码/段落标识或等价定位信息），并支持"在线预览全文"（不提供下载）：
   - 优先：原文件渲染预览（如 PDF 直接预览，DOCX/PPTX 可转为 PDF/HTML 预览）
   - 降级：若无法渲染原文件，则展示“抽取文本预览”，并提示“版式可能与原文件不一致”
3. 预览与检索边界：图片内容仅展示，不做 OCR，不参与检索。
4. 权限控制：专家仅可查看其参与任务关联证据的全文；不得浏览全库证据材料。

---

##### 4.2 系统画像（B层）：业务能力边界画像（FR-B）

###### FR-B1 画像字段与证据回链

**描述**：为每个系统维护结构化画像（以业务能力边界为中心），字段包括：
- `business_goal`
- `core_functions`（列表）
- `in_scope`
- `out_of_scope`
- `business_objects`（列表）
- `terminology`（别名/术语映射）
- `integration_points`（上下游/渠道/对接点）
- `key_constraints`（关键约束）

每个字段需可回链到证据依据（A层证据块或能力目录条目）。

**验收标准**：
1. 画像支持创建/编辑/发布（发布态用于在线评估，草稿态不参与约束）。
2. 发布画像中每个非空字段至少关联 1 条证据引用；缺少引用时该字段必须标记为“待确认”。

###### FR-B2 画像在系统识别/拆分中的注入

**描述**：当系统画像存在时，系统识别与功能拆分需根据画像状态区别处理。

**画像状态处理规则**：
- **已发布画像**：作为强约束，系统识别与功能拆分必须优先注入画像内容用于约束。
- **草稿态画像**：作为弱参考信号，具体行为：
  - **不影响相似度分数计算**：rank_score计算时不因草稿画像存在而调整
  - **不作为证据引用**：不计入"最少1条证据"的要求
  - **仅用于提示性文字**：在候选系统理由/澄清问题建议中引用，并标注"草稿态"

**验收标准**：
1. 对同一需求，在"有已发布画像"与"无画像"两种情况下，输出需体现差异（如：有画像时 `maybe` 减少或理由更充分）。
2. 当引用草稿态画像时，输出需标注"草稿态"提示，且不作为强制约束依据。

---

##### 4.3 代码扫描：能力目录/功能清单（事实层）（FR-C）

###### FR-C1 代码扫描（MVP）

**描述**：对 **Java/Spring Boot** 系统代码仓执行扫描，抽取能力目录（入口/依赖/关键词），用于系统识别召回与拆分校验。

**抽取范围（MVP，最小支持清单）**：
- API 入口：`@RestController`/`@Controller`/`@RequestMapping`/`@GetMapping`/`@PostMapping` 等
- 定时/批处理入口：Spring `@Scheduled`（Quartz/XXL-Job 等其他框架本期不保证）
- 消息入口：`@KafkaListener`、RabbitMQ `@RabbitListener`（其他 MQ 本期不保证）
- 对外依赖：`@FeignClient`（扫调用方代码）、Dubbo Consumer/Provider（扫对应侧代码）、HTTP Client、MQ Producer

产出"能力目录 JSON"，可导入作为知识条目供检索与引用。

**验收标准**：
1. 能对示例工程（Java/Spring Boot）扫描输出能力目录（至少包含 API 入口与依赖调用）。
2. 能覆盖上述“抽取范围（MVP）”中的至少 3 类入口/依赖（例如：Controller + Feign + Scheduled）。
3. 能过滤明显非业务端点（如 health/actuator），或提供可配置过滤规则并默认开启。
4. 能力目录每条条目至少包含：`entry_type/entry_id/owner/summary/keywords/related_calls`（字段允许为空但必须存在）。

###### FR-C2 能力目录用于召回与校验（不得扩范围）

**描述**：能力目录用于：
- 帮助系统识别召回候选系统；
- 帮助功能拆分校验遗漏与归属；
- 提示潜在集成/联调点。

但能力目录条目 **不得自动转成需求范围**。

**验收标准**：
1. 在功能拆分结果中，如果引用能力目录作为提示，必须标记为 `[待确认]`，且默认不计入“确定功能点”。
2. 若需求文本未出现对应业务对象/行为，系统不得仅凭能力目录自动新增功能点。

---

##### 4.4 ESB 接口服务清单：集成点索引与场景词表（FR-I）（P0）

> 背景：需求文本通常不包含接口码/交易码/服务名等强技术标识，但 ESB 清单包含 **交易名称/服务场景/提供方/调用方/状态** 等信息，可用于"集成点识别、场景词表、联调风险提示、澄清问题候选"，并为缺少代码/材料的系统提供部分兜底。  
> 重要约束：ESB 仅覆盖“跨系统集成点”，**不能**作为系统内功能清单直接拆分业务功能点（防止范围扩张）。

###### FR-I1 ESB 清单导入

**描述**：支持导入 ESB 服务治理明细表（交易/场景/提供方/调用方），形成“可检索的集成点索引”。  

**可选：系统维度统计/字典表导入（用于对齐 system_id 与补充规模信息）**：
- 字段示例：`system_id/system_name/owner/center/total_interface_count/no_call_interface_count`（其余列按 `extra` 留存）
- 约束：仅作为系统元数据与风险提示信号，不参与“系统内功能点拆分”。

**导入假设（按现状口径写死）**：
- 一行 = 一个调用方（同一交易/场景可能对应多行不同调用方）。

**最小字段（明细表）**：
- 必填：`provider_system_id`、`consumer_system_id`、`service_name`、`status`
- 推荐：`service_code`（交易码）、`scenario_code`（服务场景码）、`provider_system_name`、`consumer_system_name`、`remark`

**默认字段映射（优先按列名自动识别，可在导入时配置映射）**：
- `service_code` ⇐ `交易码`
- `scenario_code` ⇐ `服务场景码`
- `provider_system_id` ⇐ `提供方系统简称`
- `provider_system_name` ⇐ `提供方中文名称`
- `service_name` ⇐ `交易名称`
- `consumer_system_id` ⇐ `调用方系统简称`
- `consumer_system_name` ⇐ `调用方中文名称`
- `status` ⇐ `状态`
- `remark` ⇐ `备注`

**验收标准**：
1. 仅允许 `manager` 导入；无权限返回 403。
2. 支持导入格式：CSV + XLSX（允许多 Sheet，按列名自动识别；无法识别时需提示“缺少必填列”并失败）。
3. 导入后可按 `provider_system_id/consumer_system_id` 查询到该系统关联的 ESB 条目数。
4. 状态过滤默认值：
   - `valid_statuses=["正常使用"]`
   - `"废弃使用"` 导入后仅留痕，不参与默认检索/注入/统计口径（`include_deprecated=false`）。
5. 支持基本去噪：缺少必填字段的行不入库，并在导入结果中返回“跳过数量+原因摘要”。

###### FR-I2 ESB 索引检索与统计

**描述**：支持按“需求文本/关键词”检索相关 ESB 条目，并用于生成“集成点摘要”。  

**验收标准**：
1. 检索结果至少包含：`provider_system_id/consumer_system_id/service_name/scenario_code/status/similarity`（或等价排序分数）；如有名称字段需同时返回 `provider_system_name/consumer_system_name`。
2. 支持按系统过滤检索（优先按 `system_id`，兼容按 `system_name`），并支持检索范围：
   - `scope=both`（默认）：系统作为提供方或调用方均可命中
   - `scope=provider`：仅按系统作为提供方命中
   - `scope=consumer`：仅按系统作为调用方命中
3. 提供按系统维度统计（默认仅统计“有效状态”）：
   - `active_entry_count`（有效条目数）
   - `deprecated_entry_count`（废弃条目数，可选展示）
   - `active_unique_service_count`（有效交易名称去重数，可用于联调风险提示）
   - 如导入了“系统维度统计表”，额外展示：`total_interface_count/no_call_interface_count`（用于风险提示，不影响拆分范围）
4. 默认 `top_k=8`、`similarity_threshold=0.55`、`include_deprecated=false`（可配置）。

###### FR-I3 在系统识别/功能拆分中的使用（不扩范围）

**描述**：
- 系统识别阶段：ESB 的 `service_name(交易名称)` 与 `provider/consumer` 信息用于提升候选系统召回、生成更贴近业务的 `questions`，以及输出“可能涉及的集成点”提示（不要求需求中出现交易码/场景码）。
- 功能拆分阶段：当需求明确涉及“对接/同步/回调/联调/订阅”等跨系统动作时，可引用 ESB 明细条目作为 `[集成点]` 证据或提示，输出建议的“提供方/调用方/交易名称/服务场景码”。

**验收标准**：
1. ESB 只能作为“集成点证据/提示”，不得被当作系统内功能清单直接生成业务功能点。
2. 若引用 ESB 条目而需求未明确提及该对接/场景，必须标记为 `[待确认]` 且默认不计入确定估算汇总。
3. 若命中条目的 `status` 非“正常使用”（如“废弃使用”），必须标记为 `[疑似废弃]`，且默认不计入确定结论/确定集成点。

---

##### 4.5 系统识别：LLM 主导 + 证据约束 + 阻断（FR-S）

###### FR-S1 输出分层（selected/maybe/questions）

**描述**：系统识别必须输出三部分：
- `selected_systems`：可进入拆分
- `maybe_systems`：证据不足或冲突
- `questions`：最小澄清问题集（优先级排序，面向业务可回答）

**questions 数量控制**：
- 目标数量：3~8个高优先级问题
- 超出时：按"能否把 maybe 转为 selected"的优先级排序，取 Top-N
- 展示时：可按优先级分组（高/中/低），允许用户展开查看更多

**验收标准**：
1. 输出 JSON 结构固定；字段缺失视为不通过。
2. 每个 `selected_system` 必须关联至少 1 条证据引用（来自 A/B/C 任一层）。
3. `questions` 必须与 `maybe_systems` 的"不确定原因"对齐（能把 maybe 转为 selected）。
4. 当问题数量超过 8 个时，按优先级排序并高亮前 3~8 个，其余可折叠展示。
5. 默认 `selected_systems<=10`、`maybe_systems<=8`（可配置）；超出时按证据等级/召回价值截断并说明截断原因。

###### FR-S2 严肃模式阻断

**描述**：当证据不足/冲突或关键对象无法落到任何系统边界时，禁止输出确定结论。  

**验收标准**：
1. 触发阻断时：`selected_systems` 为空或仅包含证据充分的系统；其余系统进入 `maybe_systems` 并给出 `questions`。
2. 阻断提示在 UI 中明确展示：“需人工确认后再拆分/估算，当前结果不可直接用于采购结论”。

---

##### 4.6 功能拆分：类型化/模板化 + 证据标签 + 归属复核（FR-F）

###### FR-F1 拆分原则（防止范围扩张）

**描述**：
- 严格遵循需求文档，不擅自添加需求未提及功能点；
- 功能点需独立可测试；
- 跨系统交互单列（接口改造/联调/数据映射/权限对接）；
- 单点建议 0.5~5 人天，>5 必须二次拆分并说明；
- 每个功能点必须携带标签：`[归属依据] [集成点] [待确认] [非功能工作项]`；
- 数据迁移/配置/测试/上线联调等以 `[非功能工作项]` 标签形式，挂载在相关业务功能点上，计入区间上限。

**非功能工作项处理**：
- 作为标签挂在某条业务功能点上（如"客户创建"功能点带 `[非功能工作项: 测试/上线联调]` 标签）。
- 在人天估算时，非功能工作项计入区间。
- 纯非功能工作场景（如"纯数据迁移"、"纯环境配置"）不在功能点工作量评估考虑范围，转为人工评估。
- 汇总时非功能工作项不单独列支，通过标签可查看明细。

**验收标准**：
1. 生成的每条功能点必须包含上述标签（至少留空占位也可）。
2. 若出现"疑似新增范围"的功能点，必须标记为 `[待确认]` 且默认不进入确定估算汇总。
3. 非功能工作项以标签形式挂载，并在人天区间中体现影响。

###### FR-F2 类型化模板（对齐 v3.1）

**描述**：提供并使用功能类型模板：
- 单表查询、列表展示、单表CRUD、审批流程、报表统计、接口开发/对接等。

**验收标准**：
1. 拆分结果中每条功能点必须标注“功能类型”（或可推断的模板ID）。
2. 对“改造/新增”两类高频需求，拆分结构需体现其差异（改造优先影响面/对接点，新建优先对象/流程/查询报表）。

###### FR-F3 归属复核（高风险信号）

**描述**：对包含高风险信号（对账/报送/清算/征信/影像/审批引擎/统一认证/签章/短信等）或明显跨系统动词（对接/同步/回调/订阅/联调）的功能点，触发跨系统归属复核提示（不自动挪动）。  

**验收标准**：
1. 触发复核的功能点必须追加可读提示（如“疑似更偏向系统X，建议复核”）并附证据/相似度（如有）。
2. 系统不得自动改变功能点归属（保持“提示不自动挪”的原则）。

---

##### 4.7 人天输出：区间+置信度+假设（FR-E）

###### FR-E1 输出结构

**描述**：每个功能点必须输出：
- `estimate_range`（min/max）
- `confidence`（高/中/低）
- `assumptions`（未澄清假设）
- `key_factors`（上下限关键因子，如数据量/规则复杂度/集成点数量/NFR）

**区间估算算法**（混合模式）：
- **基准区间**：从功能类型模板取基准范围（如"单表CRUD"基准1-2人天）。
- **动态调整**：
  - 有明确规则时用规则（如：每多1个集成点+0.5~1人天，数据量级超预期+20~50%）
  - 无明确规则时由 LLM 依据 `assumptions`/`key_factors` 调整
- **两者结合**：最终区间 = 基准区间经规则或 LLM 调整后得出（可记录调整依据及方式）。

**验收标准**：
1. 任一功能点缺少上述字段，视为不通过。
2. 当 `confidence=低` 或 `assumptions_count>=4` 时，必须显示采购风险提示，并在汇总处标记"不可直接用于采购结论"（阈值可配置，默认值见“0.1 默认参数”）。
3. 强制低置信度规则：当 `evidence_level` 为 E0/E1 时，`confidence` 必须为“低”（不得输出中/高）。

---

##### 4.8 指标与记录（FR-M）

###### FR-M1 关键指标落库（文件存储即可）

**描述**：系统需记录用于验收与后续校准的指标：
- 系统识别准确率（Precision）、漏系统率（Recall）
- 功能拆分一致性
- 功能清单覆盖率（试点）
- 功能遗漏率（AI 漏拆，相对专家最终修正）
- 功能夸大率（AI 多拆/扩范围，相对专家最终修正）
- 低置信度占比

**计算基准**：
- **本期以“专家最终修正结果”作为基准**（用于验收与校准）；后续如接入“实际执行”数据，可增加第二套口径对比。
- 功能遗漏率 = (专家最终有而 AI 未拆分的功能点数) / (专家最终总功能点数)。
- 功能夸大率 = (AI 多拆而专家最终不需要的功能点人天) / (专家最终总人天)。

**验收标准**：
1. 完成一次任务后可在"效果统计/报告"中查看上述指标的基础统计（允许试点阶段为抽样统计）。

---

##### 4.9 兜底策略：证据等级与降级输出（FR-Z）

> 目标：解决“无代码库/无材料”的系统接入问题，并在采购严肃模式下避免输出不可控结论。

###### FR-Z1 证据等级（evidence_level）计算

**描述**：对每个候选系统与功能点计算证据等级（E0~E3），用于驱动是否允许 `selected` 与是否触发阻断。

**规则**（可配置，本期支持配置界面，以下为默认值）：
- **E3（高）**：具备 `FR-C` 能力目录（代码扫描）+（`FR-A` 证据命中 或 `FR-B` 已发布画像）。
- **E2（中）**：具备 `FR-A` 证据命中 或 `FR-B` 已发布画像，并且具备 `FR-I`（ESB）或 `FR-C`（能力目录）之一作为补证据。
- **E1（低）**：仅具备 `FR-I`（ESB），或具备 `FR-A` 但无其他补证据（能力目录/发布画像/ESB）。
- **E0（无）**：除系统名称外无任何可用证据。

**配置能力**：
- 支持通过配置界面调整证据等级判定规则（如增删证据源权重、调整阈值）。
- 配置变更需记录变更日志，便于追溯影响范围。
- 支持项目经理对单个系统的 `evidence_level` 做“结果修正”（当判定结果不合理时），但必须填写备注，且仅影响当前任务（不改变全局规则）。

**验收标准**：
1. 系统识别输出中，每个系统包含 `evidence_level` 与 `missing_evidence`（缺少哪些证据源）。
2. UI 能展示证据等级，并提示"如何补齐"。
3. 提供配置界面允许调整证据等级规则（默认值如上），并记录变更人/时间/变更前后内容。
4. 权限控制：
   - 规则配置：仅 `admin` 可修改证据等级规则；`manager` 可查看。
   - 结果修正：`manager` 可对任务内单个系统的 `evidence_level` 修正，但必须备注并记录修正日志（修正前/后、原因、操作者、时间）。

###### FR-Z2 降级输出与阻断

**描述**：当证据等级不足时，必须降级输出并控制风险。

**规则**（默认严肃模式）：
- E3/E2：允许进入 `selected_systems`，并进入拆分/估算。
- E1：默认不得进入 `selected_systems`，只能进入 `maybe_systems + questions`；如需强行确认，由 `manager` 单方确认即可，但必须低置信度并扩大区间。
- E0：阻断（不进入拆分/估算），仅输出"缺失证据+补齐清单+问题集"。

**验收标准**：
1. E1/E0 场景下，系统明确提示“当前结果不可直接用于采购结论”。
2. 阻断时仍输出可执行的最小澄清问题集（3~8个）与补齐建议（材料/代码/画像最小表单）。
3. 强行确认需留痕：记录确认人（manager）、确认时间、确认理由；并在报告中标记 `manual_override=true`。
4. 强行确认作用域：仅影响**当前任务**（不改变系统全局证据等级规则/不改变系统的默认 evidence_level）。

###### FR-Z3 最小补齐（系统画像最小表单）

**描述**：当系统缺少材料与代码时，提供“最小补齐表单”（可由 `manager` 手工填写）以解除阻断，字段至少包含：
- `in_scope/out_of_scope`
- `core_functions`（Top10）
- `business_objects`
- `integration_points`

**UI提示（本期要求）**：
- 当系统“无代码 + 无材料”且依赖最小画像进入 `selected` 时，必须在表单顶部与任务详情页显著提示（高风险标识），并在报告中保留该标识。
- 建议文案（可调整）：**“该系统未接入代码库且未上传材料，本次识别/拆分基于项目经理填写的最小画像，存在较高不确定性，请重点复核。”**

**验收标准**：
1. 未接入系统在补齐并发布最小画像后，证据等级至少达到 E2（或达到可配置门槛），允许进入 `selected`。
2. 若通过最小画像进入 `selected` 时仍为“无代码 + 无材料”，任务页与报告必须展示上述高风险提示，并在数据结构中保留标识（例如：`missing_evidence` 包含 `code/material`）。

---

#### 5. 接口需求（建议稿，供研发评审）

> 具体路径可与现有 API 规范对齐；本节提供最小接口集合，便于可验收交付。

##### 5.1 证据库（A层）

- `POST /api/v1/knowledge/evidence/import`：导入证据材料（DOCX/PPTX/PDF + 元数据）
- `POST /api/v1/knowledge/evidence/search`：检索证据块（支持 system_name 过滤/全局）
- `GET /api/v1/knowledge/evidence/stats?system_name=`：证据统计

##### 5.2 画像（B层）

- `GET /api/v1/system-profiles/{system_name}`：获取画像（含草稿/发布态）
- `PUT /api/v1/system-profiles/{system_name}`：保存画像（字段+证据引用）
- `POST /api/v1/system-profiles/{system_name}/publish`：发布画像

##### 5.3 代码扫描与能力目录

- `POST /api/v1/code-scan/run`：触发扫描（system_name + repo_path + options；`repo_path` 为后端可访问的本地仓库路径）
- `GET /api/v1/code-scan/status/{job_id}`：查询进度
- `GET /api/v1/code-scan/result/{job_id}`：获取能力目录
- `POST /api/v1/code-scan/commit/{job_id}`：将能力目录入库（知识条目）

##### 5.4 ESB 服务清单

- `POST /api/v1/knowledge/esb/import`：导入 ESB 明细表（交易/场景/提供方/调用方/状态；支持列映射）
- `POST /api/v1/knowledge/esb/search`：检索 ESB 条目（支持按 `system_id/system_name` 过滤、`scope=both|provider|consumer`、`include_deprecated`）
- `GET /api/v1/knowledge/esb/stats?system_id=&system_name=`：ESB 统计（有效条目数/废弃条目数/有效交易去重数等）

---

#### 6. 验收清单（Checklist）

##### 6.1 P0 验收（必须全部通过）

1. 证据库支持 DOCX/PPTX/PDF 导入与检索，且证据可回放（原文件可定位）。
2. 系统识别输出严格为 `selected/maybe/questions`，并实现严肃模式阻断（无证据不进 selected）。
3. 功能拆分遵循"严格按需求，不擅自扩范围"，每条功能点携带证据标签与待确认项。
4. 人天输出为"区间+置信度+假设"，低置信度时有明确采购风险提示。
5. 代码扫描能对示例工程（Java/Spring Boot）产出能力目录，并用于召回/校验（不自动扩范围）。
6. 对"无代码/无材料"的系统具备兜底：输出证据等级、按等级降级/阻断，并给出可执行的补齐清单与最小澄清问题集。
7. ESB 服务清单支持导入/检索/统计，并在系统识别/拆分中提供"交易/场景"级集成点提示（不扩范围）。
8. 证据等级规则支持配置界面调整、权限控制（仅admin可改）与变更留痕。

##### 6.2 P1 验收（建议通过）

1. 画像字段具备证据回链与发布态控制，并在系统识别/拆分中生效。
2. 指标面板能查看"功能遗漏/夸大/覆盖率"等关键指标（试点允许抽样统计）。

---

#### 7. 交付计划（建议）

- Week 1：证据库导入/检索 + 基础 UI/接口；代码扫描 MVP 能力目录（Java/Spring Boot）；ESB 清单导入/检索
- Week 2：系统识别分层输出 + 严肃模式阻断；证据等级（E0~E3）计算与 UI 提示（支持配置）；能力目录/证据注入；基础指标记录；ESB "集成点摘要"
- Week 3：功能拆分模板化 + 归属复核提示；区间估算与采购风控提示；ESB 交易/场景用于 `[集成点]` 提示（不扩范围）
- Week 4+：试点系统扩展、黄金数据集沉淀、相似案例检索/多模型集成（按 ROI 推进）
