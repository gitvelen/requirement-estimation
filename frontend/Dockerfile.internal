# 内网环境前端 Dockerfile
# 基础镜像：内网已有的 nginx 镜像
FROM nginx:latest

# 复制 nginx 配置
COPY nginx.internal.conf /etc/nginx/nginx.conf

# 复制前端构建产物
COPY build/ /usr/share/nginx/html/

# 暴露端口
EXPOSE 80

# 启动 nginx（使用前台运行模式）
CMD ["nginx", "-g", "daemon off;"]
